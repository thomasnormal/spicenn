\documentclass[11pt]{article}

\usepackage[margin=0.85in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{float}
\usepackage{array}
\usepackage{url}
\usetikzlibrary{arrows.meta,calc,fit,positioning,shapes.geometric}

\definecolor{forwardblue}{RGB}{37,99,235}
\definecolor{backred}{RGB}{190,18,60}
\definecolor{writeorange}{RGB}{217,119,6}
\definecolor{stategreen}{RGB}{22,101,52}
\definecolor{lightgray}{RGB}{246,247,249}

\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\vstate}[1]{\ensuremath{V_{\mathrm{#1}}}}
\newcommand{\mosgnd}[2]{%
  \draw[semithick] (#1,#2) -- ++(0,-0.18);
  \draw[semithick] (#1-0.18,#2-0.18) -- (#1+0.18,#2-0.18);
  \draw[semithick] (#1-0.12,#2-0.29) -- (#1+0.12,#2-0.29);
  \draw[semithick] (#1-0.06,#2-0.40) -- (#1+0.06,#2-0.40);
}
\newcommand{\moscap}[4]{%
  \node[circle, fill=black, inner sep=1.15pt, label=above:{#3}] (#4) at (#1,#2) {};
  \draw[semithick] (#1,#2) -- ++(0,-0.28);
  \draw[semithick] (#1-0.22,#2-0.28) -- (#1+0.22,#2-0.28);
  \draw[semithick] (#1-0.22,#2-0.46) -- (#1+0.22,#2-0.46);
  \draw[semithick] (#1,#2-0.46) -- ++(0,-0.28);
  \mosgnd{#1}{#2-0.74}
}
\newcommand{\drawnmos}[5]{%
  \coordinate (#5) at (#1,#2);
  \coordinate (#5D) at (#1,#2+0.42);
  \coordinate (#5S) at (#1,#2-0.42);
  \coordinate (#5G) at (#1-0.62,#2);
  \draw[semithick] (#1,#2+0.42) -- (#1,#2-0.42);
  \draw[semithick] (#1,#2+0.42) -- ++(0,0.25);
  \draw[semithick] (#1,#2-0.42) -- ++(0,-0.25);
  \draw[semithick] (#1-0.22,#2+0.34) -- (#1-0.22,#2-0.34);
  \draw[semithick] (#1-0.62,#2) -- (#1-0.22,#2);
  \node[font=\small, fill=white, inner sep=1pt, anchor=west] at (#1+0.25,#2) {#3};
  \node[font=\small, fill=white, inner sep=1pt, anchor=east] at (#1-0.65,#2) {#4};
}
\newcommand{\drawpmos}[5]{%
  \coordinate (#5) at (#1,#2);
  \coordinate (#5D) at (#1,#2-0.42);
  \coordinate (#5S) at (#1,#2+0.42);
  \coordinate (#5G) at (#1-0.68,#2);
  \draw[semithick] (#1,#2+0.42) -- (#1,#2-0.42);
  \draw[semithick] (#1,#2+0.42) -- ++(0,0.25);
  \draw[semithick] (#1,#2-0.42) -- ++(0,-0.25);
  \draw[semithick] (#1-0.24,#2+0.34) -- (#1-0.24,#2-0.34);
  \draw[semithick] (#1-0.68,#2) -- (#1-0.36,#2);
  \draw[semithick] (#1-0.36,#2) circle [radius=0.09];
  \node[font=\small, fill=white, inner sep=1pt, anchor=west] at (#1+0.25,#2) {#3};
  \node[font=\small, fill=white, inner sep=1pt, anchor=east] at (#1-0.72,#2) {#4};
}
\newcommand{\drawdiodepmos}[4]{%
  \coordinate (#4) at (#1,#2);
  \coordinate (#4D) at (#1,#2-0.42);
  \coordinate (#4S) at (#1,#2+0.42);
  \coordinate (#4G) at (#1-0.24,#2);
  \draw[semithick] (#1,#2+0.42) -- (#1,#2-0.42);
  \draw[semithick] (#1,#2+0.42) -- ++(0,0.25);
  \draw[semithick] (#1,#2-0.42) -- ++(0,-0.25);
  \draw[semithick] (#1-0.24,#2+0.34) -- (#1-0.24,#2-0.34);
  \draw[semithick] (#1-0.24,#2-0.25) -- (#1,#2-0.25);
  \draw[semithick] (#1-0.36,#2) circle [radius=0.09];
  \node[font=\small, fill=white, inner sep=1pt, anchor=west] at (#1+0.25,#2) {#3};
}

\title{Toward Fully On-Device SPICE Training of a Local-Feature MNIST Network}
\author{SPICENN architecture note}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
This note documents the SPICENN circuit architecture that currently works and
the gap that remains.  The present deck is a phase-resolved local-feature
readout network trained in one uninterrupted SPICE transient: Python supplies
only external input rails, target rails, clock/control waveforms, and final
diagnostics.  It does not read hidden state, write weights, replay reference
updates, or checkpoint neural-network state between samples.  On MNIST, a
random-initialized \(10\times 10\), block-4, stride-2, 2-channel network trained
for 6000 online updates with \(\mathrm{batch\_size}=1\) reached \(90.15\%\)
accuracy on the full 10,000-image test set, improving from a \(10.89\%\)
random baseline.

That run is not the final goal.  It used behavioral SPICE current equations for
the neuron storage and writer cells, and it used a local-PCA sample ordering
chosen to reduce input waveform switching.  The result is therefore best read
as strong architectural evidence for capacitor-held state and phase-gated
local updates, not as a completed fully device-level training demonstration.
The remaining goal is a random-init, one-transient, \(\mathrm{batch\_size}=1\)
SPICE training run with realistic sample order, no Python neural-state
updates/checkpoints, device-level neuron/write circuits, and full 10,000-image
MNIST accuracy above \(90\%\).
\end{abstract}

\section{Contract}

The target contract is narrower and stronger than ``SPICE was used somewhere
in the loop.''  During a training run:
\begin{enumerate}
  \item Python chooses the sample sequence and drives input, label, clock, and
    scalar control waveforms.
  \item All trainable neural state remains inside one SPICE transient.
  \item Each sample is processed online with \(\mathrm{batch\_size}=1\).
  \item Python does not compute hidden activations, deltas, or weight updates.
  \item Python does not checkpoint or rewrite network state between samples.
  \item The only post-run readout is diagnostic: final capacitor voltages are
    measured and evaluated for accuracy.
\end{enumerate}

The 6000-update local-PCA run satisfies this execution contract: it uses one
Xyce transient, no reference replay, and no Python-side neural-state updates.
The final 10,000-image evaluation is a NumPy diagnostic of the SPICE-trained
final state.  The training trajectory itself is not computed by NumPy or by a
Python optimizer.

This execution contract is necessary but not sufficient for the full objective.
A final-goal result must also remove unrealistic training-order assumptions and
replace the behavioral neuron/write kernels with device-level circuits or
validated transistor subcircuits.

\section{Basic Building Blocks}

The working deck is still built from a small set of circuit-level ideas, but
only the passive/state elements are hardware claims.  The non-device current
equations used in the high-accuracy prototype are now treated as scaffolding:
they identify the local relationships that the transistor cells must reproduce,
not components that should appear in a final circuit.

\begin{center}
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.27\linewidth}>{\raggedright\arraybackslash}p{0.62\linewidth}}
    \toprule
    Block & Role in the current deck \\
    \midrule
    Capacitor state &
    A voltage on a capacitor is the persistent neural state.  Local feature
    weights \(W_{bcp}\), local biases \(b_{bc}\), hidden activations \(h_{bc}\),
    hidden deltas \(\delta h_{bc}\), and readout weights \(V_{kbc}\) are all
    represented as node voltages. \\
    \addlinespace
    PWL input/target rails &
    Python drives external pixel and label waveforms.  These are inputs to the
    experiment, not computed neural state. \\
    \addlinespace
    Phase rails &
    Global rails such as activation-store, score-store, error/backward, and
    accumulation/write open local current paths at known times.  The
    high-accuracy behavioral run uses pulse-gated clocks to avoid long explicit
    PWL clock tables. \\
    \addlinespace
    MOS transconductor target &
    A source-degenerated differential pair, current mirror, switch stack, or
    pulse-coincidence writer must drive each state capacitor.  Any equation-only
    source is a prototype aid and is disqualified as hardware evidence. \\
    \addlinespace
    Signed rail pair &
    Signed quantities require differential or equivalent positive/negative
    storage.  Even if an activation is nonnegative, preactivations, synapse
    contributions, errors, hidden deltas, and writes are signed. \\
    \bottomrule
  \end{tabular}
\end{center}

The important distinction is that the current evidence is not a software
checkpoint loop, but it is also not yet a transistor demonstration.  Python only
prepares the external waveforms and reads the final state for diagnostics; the
remaining circuit task is to replace every equation-only neuron and writer path
with MOS devices that preserve the same update direction.

\section{Working Architecture}

The best explored network is a local-feature classifier rather than a dense CNN.
MNIST images are resized to \(10\times 10\) and quantized to 16 input levels.
The trainable hidden layer uses all \(4\times4\) spatial blocks at stride 2.
For a \(10\times10\) image this gives
\[
  \left(\frac{10-4}{2}+1\right)^2 = 16
\]
block positions.  Each block owns two independent local feature channels, so
the hidden representation has \(16\times2=32\) feature states.  A 10-class
readout maps those 32 feature activations to class scores.

The phrase \(\code{10x10 b4 stride2 c2}\) therefore means:
\begin{center}
  \begin{tabular}{ll}
    \toprule
    Token & Meaning \\
    \midrule
    \(\code{10x10}\) & resized input image has \(10\times10\) pixels \\
    \(\code{b4}\) & each local receptive field is \(4\times4\) pixels \\
    \(\code{stride2}\) & neighboring block origins are two pixels apart \\
    \(\code{c2}\) & two learned local feature cells per block \\
    \bottomrule
  \end{tabular}
\end{center}

Each block/channel pair has a private weight vector and bias.  There is no
weight sharing across block positions in the result reported here.  This keeps
the hardware interpretation simple: every trainable coefficient is a local
capacitor state, not a shared software kernel broadcast over many positions.

\begin{figure}[H]
  \centering
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      block/.style={draw, rounded corners=2pt, minimum height=0.95cm,
        minimum width=2.3cm, align=center, fill=lightgray, inner sep=3pt},
      statecap/.style={draw, cylinder, shape border rotate=90, aspect=0.32,
        minimum height=0.8cm, minimum width=0.65cm, align=center, fill=green!10},
      arr/.style={-{Latex[length=2mm]}, semithick},
      fwd/.style={arr, forwardblue},
      bwd/.style={arr, backred},
      wr/.style={arr, writeorange},
      lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt}
    ]
    \node[block] (x) {q16 input rails\\\(10\times10\)};
    \node[block, right=1.5cm of x] (local) {16 local blocks\\2 channels each};
    \node[block, right=1.5cm of local] (scores) {10 class\\score states};
    \node[block, right=1.5cm of scores] (err) {softmax-like\\error rails};

    \node[statecap, below=1.55cm of local, xshift=-0.75cm] (w) {\(C_W\)};
    \node[statecap, below=1.55cm of local, xshift=0.75cm] (h) {\(C_h,C_{\delta h}\)};
    \node[statecap, below=1.55cm of scores] (v) {\(C_V\)};

    \draw[fwd] (x) -- node[lbl, above] {sample} (local);
    \draw[fwd] (local) -- node[lbl, above] {hidden \(h\)} (scores);
    \draw[fwd] (scores) -- node[lbl, above] {scores} (err);

    \draw[fwd] (local) -- node[lbl, left] {store} (h);
    \draw[backred, -{Latex[length=2mm]}, semithick] (err.south) |- node[lbl, right, pos=0.25] {\(\delta\)} (h.east);
    \draw[writeorange, -{Latex[length=2mm]}, semithick] (h.south) |- node[lbl, below] {local update} (w.east);
    \draw[writeorange, -{Latex[length=2mm]}, semithick] (err.south) |- node[lbl, below] {readout update} (v.east);

    \node[draw, rounded corners=2pt, fill=white, align=left,
      text width=13.5cm, below=1.2cm of w, xshift=2.2cm, font=\footnotesize]
      {\textcolor{forwardblue}{\textbf{Blue}}: forward settling and state
       capture.\\
       \textcolor{backred}{\textbf{Red}}: output and hidden error formation.\\
       \textcolor{writeorange}{\textbf{Orange}}: phase-gated writes to
       persistent weight capacitors.};
  \end{tikzpicture}}
  \caption{Working local-feature/readout architecture.  The hidden block
  contains 32 local feature cells.  Persistent capacitor states hold local
  weights and biases, hidden activations and hidden deltas during a cycle, and
  class readout weights.  The high-accuracy behavioral run freezes output bias
  state, so there is no output-bias write path in that run.}
  \label{fig:working-architecture}
\end{figure}


\section{State And Update Equations}

The SPICE deck represents trainable state as capacitor voltages:
\[
  W_{bcp},\quad b_{bc},\quad V_{kbc},
\]
where \(b\) indexes a block position, \(c\) a local channel, \(p\) a pixel
inside the \(4\times4\) block, and \(k\) a class.  The final network has
\[
  16\cdot2\cdot16 = 512
\]
local weight states, 32 local bias states, and
\[
  10\cdot16\cdot2 = 320
\]
readout weight states.  Output biases are frozen in the high-accuracy
behavioral run, leaving 864 printed trainable state vectors.

For one sample, each local feature computes a block-local preactivation
\[
  z_{bc} = b_{bc} + \sum_p S_W(W_{bcp})\,x_{bp},
\]
then a hidden activation
\[
  h_{bc} = \tanh(z_{bc}).
\]
The best-performing branch uses a tanh-clipped transfer for local hidden
synapses,
\[
  S_W(w) = s \tanh(w/s),
\]
and a linear transfer for the class readout.  The class scores are
\[
  s_k = \sum_{b,c} V_{kbc}\,h_{bc} + o_k,
\]
with frozen \(o_k\) in the high-accuracy behavioral run.  The output error is
softmax-like:
\[
  d_k = t_k - \operatorname{softmax}(s/T)_k,
\]
with temperature \(T=2.0\).  Hidden deltas are local:
\[
  \delta h_{bc}
  =
  \left(\sum_k d_k\,V_{kbc}\right)
  (1-\tanh^2(z_{bc})).
\]

The phase-gated current sources implement direct online updates to the
capacitor voltages:
\[
  \Delta W_{bcp} = \eta\,\delta h_{bc}\,x_{bp},\qquad
  \Delta b_{bc} = \eta\,\delta h_{bc},
\]
\[
  \Delta V_{kbc} = \eta\,\alpha_V\,d_k\,h_{bc},
\]
with \(\alpha_V=0.5\) for the high-accuracy prototype.  In that prototype these
relationships were emitted as equation-only SPICE current terms.  That is useful
for proving the continuous-state schedule, but it is not hardware.  The required
next step is a MOS implementation whose measured currents have the same sign and
usable gain over the operating range.


\section{Transistor-Level Local-Feature Cell Target}

The previous behavioral diagrams are removed from this note.  They were useful
for debugging the algorithm, but they are not hardware.  The circuit target is
now the transistor-level local-feature cell in
Figs.~\ref{fig:mos-synapse-slice}--\ref{fig:mos-writer-cell}.  It is a replacement target for the
non-device equations used in the disqualified high-accuracy run, not a result
already validated at full MNIST scale.

The cell is differential throughout the signed path.  A one-pixel signed
synapse slice steers input charge into the positive or negative preactivation
rail according to differential weight capacitors.  A source-degenerated MOS
differential pair gives the tanh-like forward nonlinearity.  A replica
transconductor, or equivalently a pair of small-signal-nudged replicas, forms
the local derivative/error current.  The writer is a four-quadrant MOS
coincidence cell: two same-sign branches select the positive weight capacitor
and two opposite-sign branches select the negative weight capacitor.  Whether
the selected branch charges or discharges depends on the chosen memory
polarity; the local requirement is branch selectivity and signed update
direction.

\begin{figure}[H]
  \centering
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\large,
      lab/.style={font=\large, fill=white, inner sep=1.5pt},
      rail/.style={semithick, blue!55!black},
      gate/.style={semithick, gray!80},
      group/.style={draw, dashed, rounded corners=2pt, inner sep=9pt}
    ]
    \node[font=\bfseries\Large] at (4.8,3.6) {A. one signed synapse transconductance slice};
    \draw[rail] (-0.2,2.8) node[lab,left] {$z^+$} -- (9.8,2.8);
    \draw[rail] (-0.2,1.45) node[lab,left] {$z^-$} -- (9.8,1.45);
    \draw[semithick] (-0.2,0.10) node[lab,left] {$x^+$} -- (9.8,0.10);
    \draw[semithick] (-0.2,-0.95) node[lab,left] {$x^-$} -- (9.8,-0.95);

    \drawnmos{1.7}{0.10}{$M_{p+}$}{$x^+$}{synpp}
    \drawnmos{3.4}{-0.95}{$M_{p-}$}{$x^-$}{synpm}
    \draw[semithick] (synppD) |- (1.7,2.8);
    \draw[semithick] (synpmD) |- (3.4,1.45);
    \draw[semithick] (synppS) -- (2.55,-1.75) -- (synpmS);
    \drawnmos{2.55}{-2.75}{$N_{tail+}$}{$w^+$}{syntp}
    \draw[semithick] (2.55,-1.75) -- (syntpD);
    \draw[semithick] (syntpS) -- (2.55,-3.50);
    \mosgnd{2.55}{-3.50}

    \drawnmos{6.0}{-0.95}{$M_{n+}$}{$x^-$}{synnp}
    \drawnmos{7.7}{0.10}{$M_{n-}$}{$x^+$}{synnm}
    \draw[semithick] (synnpD) |- (6.0,2.8);
    \draw[semithick] (synnmD) |- (7.7,1.45);
    \draw[semithick] (synnpS) -- (6.85,-1.75) -- (synnmS);
    \drawnmos{6.85}{-2.75}{$N_{tail-}$}{$w^-$}{syntn}
    \draw[semithick] (6.85,-1.75) -- (syntnD);
    \draw[semithick] (syntnS) -- (6.85,-3.50);
    \mosgnd{6.85}{-3.50}

    \moscap{1.25}{-4.00}{$C_{W^+}$}{syncwp}
    \moscap{7.95}{-4.00}{$C_{W^-}$}{syncwm}
    \draw[gate] (syncwp) -| (syntpG);
    \draw[gate] (syncwm) -| (syntnG);
    \node[lab, align=center] at (2.55,-5.00)
      {\(w^+\): \(x^+\!\to z^+\), \(x^-\!\to z^-\)};
    \node[lab, align=center] at (6.85,-5.00)
      {\(w^-\): \(x^-\!\to z^+\), \(x^+\!\to z^-\)};
    \node[group, fit=(synppD) (synnmD) (syncwp) (syncwm)] {};
  \end{tikzpicture}}
  \caption{Zoomed transistor-level signed synapse slice.  A full local-feature
  cell repeats this source-coupled MOS transconductance slice for each pixel in
  the receptive field.  The \(w^-\) copy swaps the input gates, so its
  contribution is the sign reverse of the \(w^+\) copy.}
  \label{fig:mos-synapse-slice}
\end{figure}

\begin{figure}[H]
  \centering
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\large,
      lab/.style={font=\large, fill=white, inner sep=1.5pt},
      ctrl/.style={semithick, violet!80!black},
      group/.style={draw, dashed, rounded corners=2pt, inner sep=9pt}
    ]
    \node[font=\bfseries\Large] at (4.7,3.8) {B. forward differential pair and activation storage};
    \draw[semithick] (-0.1,3.15) node[lab,left] {VDD} -- (9.4,3.15);
    \drawdiodepmos{2.1}{2.35}{$P_L^+$}{fp1}
    \drawdiodepmos{6.6}{2.35}{$P_L^-$}{fp2}
    \draw[semithick] (fp1S) -- (2.1,3.15); \draw[semithick] (fp2S) -- (6.6,3.15);
    \node[circle, fill=black, inner sep=1.3pt, label=above:{$h^+$}] (fhp) at (2.1,1.45) {};
    \node[circle, fill=black, inner sep=1.3pt, label=above:{$h^-$}] (fhm) at (6.6,1.45) {};
    \draw[semithick] (fp1D) -- (fhp); \draw[semithick] (fp2D) -- (fhm);
    \drawnmos{2.1}{0.25}{$N_z^+$}{$z^+$}{fn1}
    \drawnmos{6.6}{0.25}{$N_z^-$}{$z^-$}{fn2}
    \draw[semithick] (fhp) -- (fn1D); \draw[semithick] (fhm) -- (fn2D);
    \draw[semithick] (fn1S) -- (4.35,-0.65) -- (fn2S);
    \drawnmos{4.35}{-1.6}{$N_t$}{$V_{tail}$}{fnt}
    \draw[semithick] (4.35,-0.65) -- (fntD); \draw[semithick] (fntS) -- (4.35,-2.45);
    \mosgnd{4.35}{-2.45}
    \drawnmos{0.55}{0.55}{$S_{h+}$}{$pact$}{fshp}
    \drawnmos{8.15}{0.55}{$S_{h-}$}{$pact$}{fshm}
    \moscap{0.55}{-0.75}{$C_{h^+}$}{fchp}
    \moscap{8.15}{-0.75}{$C_{h^-}$}{fchm}
    \draw[semithick] (fhp) -| (fshpD); \draw[semithick] (fhm) -| (fshmD);
    \draw[semithick] (fshpS) -- (fchp); \draw[semithick] (fshmS) -- (fchm);
    \draw[ctrl] (0.55,1.75) node[lab,above] {$pact$} -- (fshpG);
    \draw[ctrl] (8.15,1.75) node[lab,above] {$pact$} -- (fshmG);
    \node[group, fit=(fp1) (fp2) (fn1) (fn2) (fnt) (fchp) (fchm)] {};
  \end{tikzpicture}}
  \caption{Zoomed MOS forward cell matching the ngspice testbench.  The
  source-coupled NMOS pair and diode-connected PMOS loads give a bounded
  differential transfer, and phase-gated MOS switches store the differential
  activation on \(C_{h^+}\) and \(C_{h^-}\).}
  \label{fig:mos-forward-cell}
\end{figure}

\begin{figure}[H]
  \centering
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\large,
      lab/.style={font=\large, fill=white, inner sep=1.5pt},
      group/.style={draw, dashed, rounded corners=2pt, inner sep=9pt}
    ]
    \draw[semithick] (-0.2,3.2) node[lab,left] {VDD} -- (9.8,3.2);

    \node[lab] at (2.3,3.55) {\(+\epsilon\) replica};
    \drawdiodepmos{1.3}{2.4}{$P_L^+$}{hep1}
    \drawdiodepmos{3.3}{2.4}{$P_L^-$}{hem1}
    \draw[semithick] (hep1S) -- (1.3,3.2); \draw[semithick] (hem1S) -- (3.3,3.2);
    \node[circle, fill=black, inner sep=1.2pt, label=above:{$h^+_{\epsilon+}$}] (hepp) at (1.3,1.5) {};
    \node[circle, fill=black, inner sep=1.2pt, label=above:{$h^-_{\epsilon+}$}] (hepm) at (3.3,1.5) {};
    \draw[semithick] (hep1D) -- (hepp); \draw[semithick] (hem1D) -- (hepm);
    \drawnmos{1.3}{0.25}{$N_{\epsilon+}$}{$z^+\!+\epsilon$}{henp}
    \drawnmos{3.3}{0.25}{$N_{\epsilon-}$}{$z^-$}{henm}
    \draw[semithick] (hepp) -- (henpD); \draw[semithick] (hepm) -- (henmD);
    \draw[semithick] (henpS) -- (2.3,-0.6) -- (henmS);
    \drawnmos{2.3}{-1.55}{$N_{t+}$}{$V_{tail}$}{hent}
    \draw[semithick] (2.3,-0.6) -- (hentD); \draw[semithick] (hentS) -- (2.3,-2.35);
    \mosgnd{2.3}{-2.35}

    \node[lab] at (7.3,3.55) {\(-\epsilon\) replica};
    \drawdiodepmos{6.3}{2.4}{$P_L^+$}{hmp1}
    \drawdiodepmos{8.3}{2.4}{$P_L^-$}{hmm1}
    \draw[semithick] (hmp1S) -- (6.3,3.2); \draw[semithick] (hmm1S) -- (8.3,3.2);
    \node[circle, fill=black, inner sep=1.2pt, label=above:{$h^+_{\epsilon-}$}] (hmpp) at (6.3,1.5) {};
    \node[circle, fill=black, inner sep=1.2pt, label=above:{$h^-_{\epsilon-}$}] (hmpm) at (8.3,1.5) {};
    \draw[semithick] (hmp1D) -- (hmpp); \draw[semithick] (hmm1D) -- (hmpm);
    \drawnmos{6.3}{0.25}{$N_{\epsilon+}$}{$z^+\!-\epsilon$}{hmnp}
    \drawnmos{8.3}{0.25}{$N_{\epsilon-}$}{$z^-$}{hmnm}
    \draw[semithick] (hmpp) -- (hmnpD); \draw[semithick] (hmpm) -- (hmnmD);
    \draw[semithick] (hmnpS) -- (7.3,-0.6) -- (hmnmS);
    \drawnmos{7.3}{-1.55}{$N_{t-}$}{$V_{tail}$}{hmnt}
    \draw[semithick] (7.3,-0.6) -- (hmntD); \draw[semithick] (hmntS) -- (7.3,-2.35);
    \mosgnd{7.3}{-2.35}

    \node[lab, align=center] at (4.8,-2.95)
      {\(\Delta g_h =
      [(h^-_{\epsilon+}-h^+_{\epsilon+})-(h^-_{\epsilon-}-h^+_{\epsilon-})]/(2\epsilon)\)};
    \node[group, fit=(hep1) (hmm1) (hent) (hmnt)] {};
  \end{tikzpicture}}
  \caption{Zoomed MOS hidden-error characterization cell matching the ngspice
  deck.  Two matched diode-loaded forward replicas receive \(z^++\epsilon\)
  and \(z^+-\epsilon\); their differential output difference gives the local
  forward gain used to gate hidden-error transport.  This panel validates the
  transistor derivative window; the signed write selection is characterized by
  the writer cell in Fig.~\ref{fig:mos-writer-cell}.}
  \label{fig:mos-hidden-error-cell}
\end{figure}

\begin{figure}[H]
  \centering
  \resizebox{0.98\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\large,
      lab/.style={font=\large, fill=white, inner sep=1.5pt},
      group/.style={draw, dashed, rounded corners=2pt, inner sep=9pt}
    ]
    \node[font=\bfseries\Large] at (4.8,3.7) {D. four-quadrant MOS writer};
    \draw[semithick] (0.4,3.05) -- (9.2,3.05) node[lab,right] {$V_{\mathrm{write}}$};
    \moscap{2.1}{-1.95}{$C_{W^+}$}{wwp}
    \moscap{7.0}{-1.95}{$C_{W^-}$}{wwm}

    \drawpmos{0.9}{2.25}{}{$\overline{pacc}$}{wpaccA}
    \drawpmos{0.9}{1.25}{}{$\overline{x^+}$}{wxA}
    \drawpmos{0.9}{0.25}{}{$\overline{\delta h^+}$}{wdhA}
    \draw[semithick] (0.9,3.05) -- (wpaccAS);
    \draw[semithick] (wpaccAD) -- (wxAS);
    \draw[semithick] (wxAD) -- (wdhAS);
    \draw[semithick] (wdhAD) |- (wwp);

    \drawpmos{2.9}{2.25}{}{$\overline{pacc}$}{wpaccB}
    \drawpmos{2.9}{1.25}{}{$\overline{x^-}$}{wxB}
    \drawpmos{2.9}{0.25}{}{$\overline{\delta h^-}$}{wdhB}
    \draw[semithick] (2.9,3.05) -- (wpaccBS);
    \draw[semithick] (wpaccBD) -- (wxBS);
    \draw[semithick] (wxBD) -- (wdhBS);
    \draw[semithick] (wdhBD) |- (wwp);

    \drawpmos{6.0}{2.25}{}{$\overline{pacc}$}{wpaccC}
    \drawpmos{6.0}{1.25}{}{$\overline{x^+}$}{wxC}
    \drawpmos{6.0}{0.25}{}{$\overline{\delta h^-}$}{wdhC}
    \draw[semithick] (6.0,3.05) -- (wpaccCS);
    \draw[semithick] (wpaccCD) -- (wxCS);
    \draw[semithick] (wxCD) -- (wdhCS);
    \draw[semithick] (wdhCD) |- (wwm);

    \drawpmos{8.0}{2.25}{}{$\overline{pacc}$}{wpaccD}
    \drawpmos{8.0}{1.25}{}{$\overline{x^-}$}{wxD}
    \drawpmos{8.0}{0.25}{}{$\overline{\delta h^+}$}{wdhD}
    \draw[semithick] (8.0,3.05) -- (wpaccDS);
    \draw[semithick] (wpaccDD) -- (wxDS);
    \draw[semithick] (wxDD) -- (wdhDS);
    \draw[semithick] (wdhDD) |- (wwm);

    \node[lab, align=center] at (1.9,-3.2)
      {\(W^+\) charge branches\\\(x^+\delta h^+\), \(x^-\delta h^-\)};
    \node[lab, align=center] at (7.0,-3.2)
      {\(W^-\) charge branches\\\(x^+\delta h^-\), \(x^-\delta h^+\)};
    \node[group, fit=(wwp) (wwm) (wpaccAS) (wdhDD)] {};
  \end{tikzpicture}}
  \caption{Zoomed MOS four-quadrant writer.  Each update path is a stack of
  active-low phase, input, and hidden-error PMOS devices from \(V_{\mathrm{write}}\)
  to a weight storage capacitor.  Same-sign coincidences charge \(C_{W^+}\);
  opposite-sign coincidences charge \(C_{W^-}\).}
  \label{fig:mos-writer-cell}
\end{figure}

The expected small-signal relationships are transistor relationships, not
equation-only source definitions.  With the simple Level-1 devices used in the
testbench the forward transfer is closer to a bounded hard-tanh than a
subthreshold tanh, but the required signs are the same:
\[
  I_{h^+}-I_{h^-}
  \approx I_b\tanh\!\left(\frac{\kappa(V_{z^+}-V_{z^-})}{2V_T}\right),
\]
\[
  \delta h \propto r\,
    \frac{\partial (V_{h^-}-V_{h^+})}{\partial V_{z^+}},
\]
\[
  I_{W^+}\propto \overline{pacc}\,(\overline{x^+}\,\overline{dh^+}
    + \overline{x^-}\,\overline{dh^-}),\qquad
  I_{W^-}\propto \overline{pacc}\,(\overline{x^+}\,\overline{dh^-}
    + \overline{x^-}\,\overline{dh^+}).
\]
This is the circuit family that should replace the non-device prototype before
any future headline MNIST claim.

\subsection{Ngspice Single-Cell Characterization}

Each MOS panel above now has a matching ngspice testbench and plotted output.
The command
\[
  \code{python3 spice/run\_mos\_cell\_characterization.py}
\]
emits small Level-1 MOS decks under
\(\code{results/mos\_cell\_characterization/}\), runs them with ngspice, and
writes the plots in
Figs.~\ref{fig:mos-synapse-ngspice}--\ref{fig:mos-writer-retention-ngspice}.
These decks use only MOS devices, capacitors, resistors, and independent
testbench sources; there are no behavioral current sources in these cell
checks.  They are not process-calibrated device claims and they are not
MNIST-scale training evidence.  They are circuit-level sanity checks for the
sign, monotonicity, and selectivity relationships that a transistor replacement
must satisfy before it can replace the current behavioral prototype.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_slice_ngspice.png}
  \caption{Ngspice characterization of the signed synapse slice.  The top panel
  is the DC transconductance check using the mathematical contribution
  convention: a high \(w^+\) rail gives a positive contribution for
  \(x^+>x^-\), while a high \(w^-\) rail reverses the sign.  The bottom panel
  repeats the same gate-swapped MOS slice with real preactivation capacitors;
  during the weight-enabled pulse, \(w^+\) raises stored \(z^- - z^+\), while
  \(w^-\) lowers it.  When equal \(w^+\) and \(w^-\) copies drive the same
  summing capacitors, the differential state stays near zero, and all capacitor
  states hold after the pulse closes.}
  \label{fig:mos-synapse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_weighted_ngspice.png}
  \caption{Ngspice weighted-cancellation characterization of the signed MOS
  synapse slice.  The same \(x^+>x^-\) differential input drives paired
  positive- and negative-weight transconductance copies into shared summing
  capacitors while the \(W^+\) and \(W^-\) tail-gate amplitudes are varied.
  The endpoint cases match the one-rail \(W^+\) and \(W^-\) signs, the balanced
  case cancels, and the unequal cases leave the expected partial positive or
  negative stored preactivation.  The held samples overlay the post-write
  samples, so the partial-cancellation state is retained after the tail pulses
  close.}
  \label{fig:mos-synapse-weighted-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_weight_gate_ngspice.png}
  \caption{Ngspice analog tail-gate characterization of the signed MOS synapse.
  A fixed \(x^+>x^-\) input drives matched positive- and negative-weight
  transconductance copies while the NMOS tail-gate voltage is swept from off to
  strongly enabled.  Below threshold both copies are effectively quiet.  Above
  threshold the \(W^+\) contribution grows monotonically positive, and the
  gate-swapped \(W^-\) copy mirrors the same magnitude with the opposite sign.
  This is the direct transistor-level check that a stored analog weight voltage
  can scale a synapse contribution continuously, not just select an on/off
  branch.}
  \label{fig:mos-synapse-weight-gate-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_mismatch_ngspice.png}
  \caption{Ngspice threshold-mismatch characterization of the signed MOS
  synapse.  The \(z^+\) branch input transistor is modeled with \(V_{TO}\)
  shifted by \(-20\), \(0\), or \(+20\) mV while the complementary branch is
  nominal.  The \(W^+\) contribution stays monotone, keeps a single zero
  crossing, and retains useful current swing, but the crossing shifts with
  device threshold.
  The gate-swapped \(W^-\) copy mirrors the offset with the opposite sign.  This
  exposes the analog synapse offset budget that differential transport and
  training margins must tolerate.}
  \label{fig:mos-synapse-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_common_mode_ngspice.png}
  \caption{Ngspice input common-mode margin characterization of the signed MOS
  synapse slice.  Three matched positive- and negative-weight copies sweep
  \(x^+-x^-\) while the input common-mode is held at \(0.75\), \(0.90\), and
  \(1.05\) V.  The \(w^+\) branch keeps the expected sign over the full
  differential input range, and the gate-swapped \(w^-\) branch keeps the
  opposite sign.  The zero-crossing offset remains about \(2.1\) \(\mu\)A
  while the peak contribution swing remains above \(460\) \(\mu\)A in this
  Level-1 testbench, so the signed synapse is not relying on one exact
  activation common-mode point.}
  \label{fig:mos-synapse-common-mode-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_forward_pair_ngspice.png}
  \caption{Ngspice characterization of the MOS forward differential pair and
  activation store.  The top panel is the DC transfer under fixed common-mode
  differential drive.  The diode-connected load is voltage-inverting, so the
  useful signed load voltage is plotted as \(h^- - h^+\).  The bottom panel is
  a transient storage check using the same differential drive convention: when
  \(pact\) is high the switch tracks the current activation onto \(C_h\), and
  when \(pact\) closes the stored differential activation is held.}
  \label{fig:mos-forward-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_forward_store_sweep_ngspice.png}
  \caption{Ngspice magnitude sweep for the MOS forward-store path.  Five
  static diode-loaded forward-pair copies drive real activation capacitors
  through the same \(pact\) pass switches while \(z^+-z^-\) is varied.  The
  stored differential activation is centered at zero, monotone in preactivation,
  approximately odd-symmetric, and overlays both the immediate load output and
  the post-\(pact\) held value.  This checks that the activation storage path
  carries graded analog magnitude, not only positive/negative sign.}
  \label{fig:mos-forward-store-sweep-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_forward_mismatch_ngspice.png}
  \caption{Ngspice threshold-mismatch characterization of the MOS forward
  differential pair.  The left input transistor is modeled with \(V_{TO}\)
  shifted by \(-20\), \(0\), or \(+20\) mV while the right transistor remains
  nominal.  The transfer stays monotone and retains useful swing, but the zero
  crossing moves by the same order as the threshold skew.  This is an offset
  budget check, not a calibrated mismatch claim: a full cell will need
  differential margins, reset/common-mode control, or calibration if offsets
  approach the activation window.}
  \label{fig:mos-forward-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_forward_common_mode_ngspice.png}
  \caption{Ngspice common-mode margin characterization of the same MOS forward
  pair.  Three transistor-level copies sweep \(z^+-z^-\) while their input
  common-mode rails are held at \(0.75\), \(0.90\), and \(1.05\) V.  The signed
  load transfer remains centered to within about \(1\) mV, keeps monotone
  bounded swing, and preserves roughly unit small-signal gain near
  \(z^+\approx z^-\).  This validates the local bias window used by the
  forward-store and hidden-derivative replica tests; it is not a process-wide
  PVT claim.}
  \label{fig:mos-forward-common-mode-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_forward_chain_ngspice.png}
  \caption{Ngspice synapse-to-forward-store integration characterization.  A
  signed MOS synapse writes real preactivation summing capacitors, the
  diode-loaded MOS forward pair reads those same capacitors, and \(pact\)
  samples the resulting activation onto \(C_h\).  Because the summing caps are
  voltage-inverting, a positive current contribution appears as
  \(z^- - z^+>0\); the forward pair therefore uses crossed gates so that its
  \(z^+-z^-\) input equals the stored contribution voltage.  The positive and
  negative synapse copies produce about \(\pm 0.10\) V of preactivation, the
  forward load preserves the sign, and the activation storage holds the sampled
  result after \(pact\) closes.}
  \label{fig:mos-synapse-forward-chain-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_forward_cycle_ngspice.png}
  \caption{Ngspice two-cycle reuse characterization for the same synapse and
  forward-store path.  A first sample writes a positive preactivation and
  stores a positive activation.  A complementary reset then clears both the
  summing capacitors and the activation capacitors without Python forcing those
  nodes.  The same physical capacitors are then reused for a gate-swapped
  negative-weight sample, and \(pact\) stores the opposite activation sign.
  This checks the per-sample reset/write/store boundary that the isolated reset
  and one-shot chain plots do not cover by themselves.}
  \label{fig:mos-synapse-forward-cycle-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_synapse_forward_phase_timing_ngspice.png}
  \caption{Ngspice synapse-to-forward-store \(pact\)-timing characterization.
  One signed synapse writes the preactivation capacitors while separate matched
  forward-store copies sample before, during, and after the write.  The
  pre-write \(pact\) pulse stores no signed activation, the write-edge pulse
  captures a partial activation, and the overlap/late/gap samples converge once
  the preactivation and forward load settle.  The small load transient in the
  representative settled-gap copy is the local sampling kick from its own MOS
  pass switches; it recovers before the stored value is held.}
  \label{fig:mos-synapse-forward-phase-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_ngspice.png}
  \caption{Ngspice characterization of the hidden-error path.  The top panel
  uses two matched diode-loaded MOS forward replicas nudged by \(+10\) mV and
  \(-10\) mV in differential input at fixed common mode; their finite difference
  gives the local small-signal derivative window.  The bottom panel is a separate
  transient sign-selection check: MOS pass switches controlled by \(r^+\) or
  \(r^-\) store opposite differential hidden-error polarities on capacitors and
  hold them after the backward phase closes.}
  \label{fig:mos-hidden-error-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_gain_store_ngspice.png}
  \caption{Ngspice storage characterization for the hidden-error derivative
  proxy.  MOS pass switches sample the \(+\epsilon\) and \(-\epsilon\)
  forward-replica outputs onto capacitors at three preactivation operating
  points; the plotted value is the finite difference computed from those stored
  capacitor voltages.  The sampled derivative proxy is large near
  \(z^+\approx z^-\), much smaller in both saturated regions, and is retained
  after the sampling phase closes.  This validates storage of the replica
  derivative window; it is not a full hidden-error subtractor circuit.}
  \label{fig:mos-hidden-error-gain-store-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_sign_store_ngspice.png}
  \caption{Ngspice cross-connected MOS hidden-error storage characterization.
  Two forward-replica pairs are driven at \(z^+-z^-+\epsilon\) and
  \(z^+-z^--\epsilon\).  During \(pbwd\), pass-switch networks controlled by
  \(r^+\) connect \(h^-_{+\epsilon}\) and \(h^+_{-\epsilon}\) to
  \(\delta^+\), while connecting \(h^+_{+\epsilon}\) and \(h^-_{-\epsilon}\)
  to \(\delta^-\); the \(r^-\) copy swaps those connections.  The storage
  capacitors therefore form the finite-difference sign directly by charge
  sharing, rather than relying on a Python subtractor.  The center operating
  point stores a large signed hidden error, both saturated operating points are
  attenuated, and the stored sign holds after \(pbwd\) closes.}
  \label{fig:mos-hidden-error-sign-store-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_nudge_sweep_ngspice.png}
  \caption{Ngspice nudge-magnitude characterization of the same MOS
  hidden-error storage path.  At the center operating point, matched
  \(+\epsilon\) and \(-\epsilon\) diode-loaded forward replicas feed the
  cross-connected pass-switch network while \(\epsilon\) is swept.  Zero nudge
  stores no hidden-error differential.  Larger nudges produce a graded
  \(r^+\) step and a mirrored \(r^-\) step, and both sampled values overlay
  their held values after \(pbwd\) closes.  This checks that the hidden-error
  storage path carries continuous magnitude information, not only a binary
  sign.}
  \label{fig:mos-hidden-error-nudge-sweep-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_mismatch_ngspice.png}
  \caption{Ngspice threshold-mismatch characterization of the hidden-error
  finite-difference replicas.  The \(z^+\) input devices in both nudged copies
  use \(V_{TO}=0.53\), \(0.55\), and \(0.57\) V while the complementary input
  devices stay nominal.  The derivative proxy remains nonnegative and near
  unity across the active region; mismatch shifts both \(0.5\)-gain window
  edges by roughly \(20\)--\(30\) mV without collapsing the window width.  This
  is the hidden-error counterpart to the forward/synapse mismatch plots:
  offsets move the local derivative window, so a full transistorized cell needs
  margin or calibration, but the tested offsets do not flip the backward sign.}
  \label{fig:mos-hidden-error-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_error_common_mode_ngspice.png}
  \caption{Ngspice input common-mode characterization of the hidden-error
  finite-difference replicas.  Matched nudged replica pairs are run at
  \(V_{CM}=0.75\), \(0.90\), and \(1.05\) V while \(z^+-z^-\) ramps through the
  active region.  The center derivative gain stays near unity at all three
  biases.  The active window is bias-dependent: the \(0.5\)-gain width is about
  \(0.48\), \(0.67\), and \(0.78\) V respectively, and the high-common-mode
  case has more edge leakage.  This plot makes the hidden-error bias margin
  explicit rather than assuming that the nominal common-mode point is free.}
  \label{fig:mos-hidden-error-common-mode-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_chain_ngspice.png}
  \caption{Ngspice hidden-error-store to writer/readback integration
  characterization.  The cross-connected MOS replica network first stores
  positive and negative hidden-error signs on capacitors.  Those stored analog
  rails then drive the PMOS writer gates directly, together with a positive
  activation rail and the \(pacc\) phase.  The \(r^+\) copy steers the update
  toward \(W^+>W^-\); the \(r^-\) copy steers it toward \(W^- > W^+\).  The
  same written rails are continuously read through positive and gate-swapped
  negative synapse slices, where they produce opposite signed currents.  This
  closes the backward/update boundary at the single-cell level without an ideal
  comparator or Python-side update source.}
  \label{fig:mos-hidden-writer-chain-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_quadrants_ngspice.png}
  \caption{Ngspice four-quadrant hidden-error-driven writer characterization.
  The same cross-connected hidden-error store feeds writer copies under both
  positive and negative activation rail polarities.  With the PMOS coincidence
  writer, the complementary low rail is the active analog gate: \(x^+\) uses
  \(h^-\), \(x^-\) uses \(h^+\), \(r^+\) uses \(\delta^-\), and \(r^-\) uses
  \(\delta^+\).  Same-sign products \(x^+r^+\) and \(x^-r^-\) move
  \(W^+-W^-\) positive, while opposite-sign products \(x^+r^-\) and
  \(x^-r^+\) move it negative.  This completes the sign-family check for the
  hidden-error-to-writer boundary using only MOS switches, capacitors, and
  independent phase/input sources.}
  \label{fig:mos-hidden-writer-quadrants-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_magnitude_ngspice.png}
  \caption{Ngspice hidden-error magnitude to writer-magnitude characterization.
  Several matched MOS replica/store copies create different \(r^+\) hidden-error
  magnitudes, then use the stored capacitor rails directly as PMOS writer gates
  under the same positive activation and \(pacc\) phase.  Zero stored error
  leaves the differential weight unchanged.  Larger stored errors produce
  larger \(W^+-W^-\) steps, and the written values hold after the write phase.
  This closes the graded backward/update path: the transistor-level check is no
  longer only ``error sign selects a branch,'' but also ``stored error magnitude
  scales the local write.''}
  \label{fig:mos-hidden-writer-magnitude-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_phase_timing_ngspice.png}
  \caption{Ngspice hidden-error-to-writer phase-timing characterization.  Several
  writer copies share the same stored \(r^+\) hidden-error capacitor rails while
  their active-low \(pacc\) pulses are shifted before, during, and after the
  \(pbwd\) storage phase.  A pulse before hidden-error storage creates only a
  tiny signed residue, the storage-edge pulse is slightly smaller than the
  fully settled update, and later overlap/gap timings converge to the same
  weight differential.  This does not claim arbitrary clock overlap is safe; it
  identifies the local sequencing margin for this Level-1 MOS writer and
  hidden-error store.}
  \label{fig:mos-hidden-writer-phase-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_mismatch_ngspice.png}
  \caption{Ngspice PMOS-threshold mismatch characterization of the integrated
  hidden-error-store to writer boundary.  The hidden-error rails are generated
  by the nominal MOS finite-difference/store network and then used directly as
  analog writer gates while the writer PMOS threshold is swept through
  \(V_{TO}=-0.50\), \(-0.55\), and \(-0.60\) V.  The signed \(r^+\) and \(r^-\)
  weight differentials remain symmetric and keep the correct sign while their
  gain decreases with weaker PMOS corners.  The complementary rails are not
  perfectly quiet because the stored analog ``off'' gate is around \(1.09\) V,
  not a rail-high logic level; the regression therefore checks selected-versus-
  complementary contrast rather than a digital off assumption.  This is an
  important robustness finding for the transistor path: a direct analog writer
  can work in this window, but a later larger cell may need level restoration,
  a higher hidden-error common mode, or a different writer bias to reduce
  complementary leakage.}
  \label{fig:mos-hidden-writer-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_gate_bias_ngspice.png}
  \caption{Ngspice activation-gate bias sweep for the same integrated
  hidden-error-store to writer boundary.  The hidden-error store is unchanged;
  each writer copy receives a different active-low \(h^-\) gate for the
  positive activation branch.  Raising \(h^-\) weakens the selected and
  complementary PMOS stacks together.  Absolute complementary leakage falls,
  but the useful net update and the selected/complement ratio also fall.  The
  measured selected/complement ratio drops from about \(2.18\) at
  \(h^-=0.86\) V to \(1.69\) at \(h^-=1.10\) V, while the net \(r^+\) update
  drops from about \(14.6\) mV to \(5.8\) mV.  This rules out a simple
  activation-gate bias shift as the clean fix for complementary writer
  leakage.  The next transistor candidate should restore the hidden-error
  gates, add a local inhibit/differential writer branch, or change the writer
  topology so the unselected rail is actively suppressed rather than merely
  weakened.}
  \label{fig:mos-hidden-writer-gate-bias-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_ngspice.png}
  \caption{Ngspice restored hidden-error gate characterization for the integrated
  writer boundary.  The same MOS hidden-error store first creates about
  \(\pm98.5\) mV of differential error.  A skewed CMOS inverter restorer then
  converts those analog capacitor rails into active-low writer gates before
  \(pacc\).  In this Level-1 sizing, the selected restored gate is about
  \(0.354\) V and the complementary restored gate is about \(1.756\) V, compared
  with the direct analog gates near \(0.995\) V and \(1.093\) V.  The result is
  a much cleaner local write: the complementary rail step falls from about
  \(11.8\) mV in the direct writer to the nV scale, while the selected rail
  remains incremental at about \(65.0\) mV and the \(r^+\)/\(r^-\) signed
  updates stay symmetric.  This is the first MOS-only hidden-error writer
  variant in the note that makes the complement path behave like a real
  unselected branch rather than a weakly selected analog branch.}
  \label{fig:mos-hidden-writer-restored-gate-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_mismatch_ngspice.png}
  \caption{Ngspice threshold-offset characterization of the restored
  hidden-error gate writer.  The nominal hidden-error store is reused while the
  skewed CMOS restorer devices are perturbed.  The complement path remains
  strongly suppressed in all tested cases, but the selected-side NMOS threshold
  margin is the critical weakness: a \(+70\) mV selected-NMOS offset raises the
  selected restored gate to about \(1.68\) V and collapses the write to zero.
  Weakening only the complementary pull-up leaves the useful signed update near
  the nominal \(65\) mV step.  This is a better failure mode than a sign flip,
  but it means the restored writer needs selected-side threshold margin,
  calibration, larger stored error swing, or a less abrupt restoring topology
  before it can be treated as robust device-level training hardware.}
  \label{fig:mos-hidden-writer-restored-gate-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_strength_ngspice.png}
  \caption{Ngspice restorer-NMOS sizing sweep for the selected-weak threshold
  corner in Fig.~\ref{fig:mos-hidden-writer-restored-gate-mismatch-ngspice}.
  The weakest restored gate reproduces the failure: the selected gate remains
  near \(1.68\) V and the signed write is essentially zero.  Moderate NMOS
  widths create a usable window: \(18\)--\(36\,\mu\mathrm{m}\) pulls the
  selected gate below \(0.25\) V, keeps the complement gate above \(1.61\) V,
  and restores a signed update of about \(\pm65\) mV with nV-scale complement
  motion.  Over-sizing is not robust; by \(48\,\mu\mathrm{m}\) the complement
  gate also turns on, both writer rails move by about \(65\) mV, and the
  differential update cancels.  The transistor lesson is that the restored
  hidden-error writer needs explicit restorer sizing margin, not merely a
  stronger pull-down device.}
  \label{fig:mos-hidden-writer-restored-gate-strength-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_swing_ngspice.png}
  \caption{Ngspice stored-error swing sweep for the same selected-weak
  restored writer, using the moderate \(W_N=24\,\mu\mathrm{m}\) restorer
  sizing from Fig.~\ref{fig:mos-hidden-writer-restored-gate-strength-ngspice}.
  A small finite-difference nudge stores only about \(29.5\) mV of hidden-error
  differential, leaves the selected restored gate near \(1.25\) V, and produces
  no signed write.  Once the stored error reaches about \(49\) mV, the selected
  gate crosses the skewed inverter trip point, drops to about \(0.31\) V, and
  the writer produces the full \(\pm65\) mV signed step while the complement
  branch remains at the nV scale.  Larger stored errors continue to improve
  gate margin but no longer increase the write amplitude in this sizing.  This
  makes the restored writer robust as a sign-cleaning stage, but it also means
  magnitude-preserving learning needs either a softer restorer, pulse-width or
  current scaling after restoration, or an upstream analog-magnitude path.}
  \label{fig:mos-hidden-writer-restored-gate-swing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_ngspice.png}
  \caption{Ngspice hybrid restored-enable plus analog-error writer.  The
  restored hidden-error rail gates a branch-select PMOS, while the original
  stored analog hidden-error capacitor remains in series as a second PMOS gate.
  With \(W_N=18\,\mu\mathrm{m}\), zero hidden error leaves both differential
  weight rails balanced.  For nonzero stored error, the complement restored
  gate stays above \(1.70\) V and the complement rail step remains at the nV
  scale, while the selected signed update grows with hidden-error magnitude:
  about \(27.7\), \(49.2\), \(55.1\), and \(60.5\) mV for
  \(\epsilon=30,60,100,140\) mV.  This is the first restored writer variant
  here that combines sign-clean complement suppression with a graded local
  write, although the transfer is compressed by the series PMOS stack and still
  needs mismatch/noise characterization.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_mismatch_ngspice.png}
  \caption{Ngspice threshold-corner characterization of the hybrid restored
  writer at a fixed \(\epsilon=100\) mV hidden-error nudge.  Selected-restorer
  and complement-restorer threshold shifts leave the selected gate near
  \(0.20\)--\(0.23\) V and the complement gate above \(1.71\) V, so the
  complement rail remains suppressed at the nV scale.  Writer PMOS threshold
  corners mainly change update gain: the signed step is about \(37.4\) mV in
  the weak-writer corner, \(55.1\) mV nominally, and \(75.7\) mV in the
  strong-writer corner.  The combined selected-weak, complement-strong,
  writer-weak corner still writes the correct sign at about \(37.3\) mV.  This
  supports the hybrid topology as the current best MOS writer candidate; the
  next robustness gap is dynamic noise and phase-overlap sensitivity rather
  than static threshold sign failure.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_timing_ngspice.png}
  \caption{Ngspice phase-overlap characterization of the hybrid restored-enable
  plus analog-error writer.  A single MOS hidden-error store creates about
  \(98.5\) mV of \(r^+\) differential error, the restored select gates settle
  to about \(0.196\) V selected and \(1.731\) V complementary, and six matched
  writer copies sweep the active-low \(pacc\) window.  A true pre-store write
  pulse is quiet, with only a \(6\) nV signed residue.  A pulse that overlaps
  the start of backward storage by about \(50\) ns creates a small stray
  \(2.3\) mV write, roughly \(8.6\%\) of the full update.  Pulses beginning at
  the storage edge or later converge to the full \(26.4\)--\(27.0\) mV signed
  write while the complement rail stays at the nV scale.  The sequencing rule
  for this Level-1 writer is therefore concrete: keep \(pacc\) out of the
  pre-store interval, but it can begin at or after the hidden-error storage edge
  once the restored gates have settled.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_noise_ngspice.png}
  \caption{Ngspice charge-kick disturbance characterization of the hybrid
  restored-enable plus analog-error writer.  Each copy first stores the same
  \(98.5\) mV \(r^+\) hidden-error differential, then receives deterministic
  current-pulse kicks on the two hidden-error capacitors before \(pacc\).  The
  \(\pm25\) mV common-mode kicks preserve the stored differential and change
  only write gain, moving the signed update from the nominal \(55.1\) mV to
  about \(46.2\) mV and \(63.1\) mV.  A \(+25/-25\) mV differential boost
  raises the stored differential to \(158.5\) mV and strengthens the write to
  \(64.9\) mV.  A \(-25/+25\) mV destructive differential kick leaves
  \(38.5\) mV of positive stored error and still writes the correct sign at
  \(45.1\) mV with nV-scale complement motion.  A larger \(-50/+50\) mV
  destructive kick crosses the sign boundary, leaving \(-21.5\) mV stored
  error and producing only a small wrong-sign residue of about \(-0.38\) mV.
  The practical noise rule is therefore not ``the restored writer is immune to
  disturbance,'' but a concrete margin: keep differential post-store
  disturbance below roughly half the stored hidden-error swing, or increase the
  stored-error/restorer margin.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-noise-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_activation_ngspice.png}
  \caption{Ngspice activation-side magnitude characterization of the hybrid
  restored writer.  A single stored hidden-error rail is reused while matched
  writer copies sweep the active-low positive-activation PMOS gate \(h^-\).
  The stored \(r^+\) differential remains \(98.5\) mV, the restored select
  gates stay near \(0.196\) V selected and \(1.731\) V complementary, and the
  complement weight rail remains at the nV scale for every activation gate.
  Raising \(h^-\) weakens the selected PMOS stack monotonically: the signed
  write falls from about \(59.3\) mV at \(h^-=0.84\) V to \(33.4\) mV at
  \(h^-=1.10\) V.  This closes the product-path family check for the hybrid
  writer: after restoration supplies branch selectivity, the original analog
  hidden-error rail and the activation-side gate both still modulate write
  magnitude.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-activation-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_activation_store_ngspice.png}
  \caption{Ngspice stored-activation-gate characterization for the hybrid
  restored writer.  The activation operand is no longer an ideal DC sweep:
  \(h^-\) is a capacitor sampled through a complementary MOS pass gate.  The
  first sample stores an active-low \(0.9200\) V gate and holds it until
  \(pacc\), producing a \(25.7\) mV \(W^+ - W^-\) update from the same
  \(98.5\) mV \(r^+\) hidden-error rail.  The second sample overwrites the
  activation capacitor with an inactive-high \(1.4500\) V gate before a second
  \(pacc\) pulse on the same weight pair.  That inactive reuse pulse changes
  the signed weight by only about \(0.004\,\mu\mathrm{V}\), while the
  complement weight rail moves about \(0.01\,\mu\mathrm{V}\).  This closes the
  activation-operand lifecycle gap left by
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-activation-ngspice}:
  the hybrid writer can be gated by a sampled activation capacitor, and a later
  inactive sample suppresses the update without Python clearing or replacing
  the weight state.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-activation-store-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_activation_timing_ngspice.png}
  \caption{Ngspice activation-store timing characterization for the hybrid
  restored writer.  Matched writer copies share the same \(98.5\) mV stored
  \(r^+\) hidden-error rail, the same restored branch-select gates
  (\(0.196\) V selected, \(1.731\) V complementary), and the same sampled
  activation source; only the \(pacc\) window changes.  A \(pacc\) pulse before
  activation sampling is quiet (\(\sim 2\) nV signed residue).  Starting at the
  sample edge catches the activation capacitor before it has settled and gives
  a partial \(16.7\) mV signed write.  Once sampling overlaps or precedes
  \(pacc\) with margin, the signed write settles to \(24.33\) mV across the
  overlap, post-sample, and settled-gap cases, while the complement \(W^-\)
  rail changes only at the nV scale.  The concrete phase rule is therefore:
  store the activation operand before asserting \(pacc\), or deliberately
  budget the sample-edge partial-write behavior.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-activation-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_width_ngspice.png}
  \caption{Ngspice \(pacc\)-pulse-width characterization for the hybrid
  restored writer.  The deck keeps one shared MOS hidden-error store, one
  shared restored branch selector, and one shared activation capacitor sampled
  through the MOS pass gate from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-activation-store-ngspice};
  matched writer copies differ only in the active-low \(pacc\) pulse width.
  The sampled activation holds from \(0.9199997\) V to \(0.9199979\) V through
  the write fanout.  The signed \(W^+ - W^-\) step scales almost exactly
  linearly over the tested window: \(2.91\), \(5.59\), \(10.94\), \(21.65\),
  and \(43.06\) mV for \(40\), \(80\), \(160\), \(320\), and \(640\) ns,
  respectively, with a linear-fit \(R^2\) indistinguishable from one at the
  plotted precision.  The complement \(W^-\) rail stays at the nV scale.  This
  gives the current hybrid writer a transistor-level learning-rate knob:
  update magnitude can be set by \(pacc\) pulse width without reintroducing
  complement-branch leakage in this operating range.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-width-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_product_ngspice.png}
  \caption{Ngspice four-quadrant product-routing characterization for the
  hybrid restored writer.  The deck stores matched \(e^+\) and \(e^-\)
  hidden-error rails of \(\pm98.5\) mV, restores each selected error sign to
  an active-low writer gate near \(0.196\) V, and samples exactly one
  activation sign per quadrant through MOS pass gates.  The writer routing is
  the sign-split local product rule:
  \(W^+\leftarrow a^+e^+ + a^-e^-\) and
  \(W^-\leftarrow a^+e^- + a^-e^+\).  The measured signed updates are
  \(+22.99\), \(-22.99\), \(-22.99\), and \(+22.99\) mV for
  \(a^+e^+\), \(a^+e^-\), \(a^-e^+\), and \(a^-e^-\), respectively, while the
  inactive rail in each quadrant moves only about \(3\) nV.  This closes the
  signed-activation gap left by the single-ended activation plots: the same
  hybrid writer topology can route all four signs of the local product using
  sampled activation rails and stored/restored error rails.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-product-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_bias_ngspice.png}
  \caption{Ngspice local-bias update characterization for the hybrid restored
  writer.  The local-feature cell needs a bias update
  \(\Delta b\propto e\) in addition to the activation-gated weight update.
  This deck stores matched \(e^+\) and \(e^-\) hidden-error rails of
  \(\pm98.5\) mV, restores each selected sign to the same active-low writer
  gate near \(0.196\) V, and writes persistent \(B^+/B^-\) capacitors through
  phase/restored-select/analog-error PMOS stacks without an activation operand.
  The \(e^+\) branch moves \(B^+\) by \(34.54\) mV while \(B^-\) moves only
  about \(3\) nV; the \(e^-\) branch symmetrically moves \(B^-\) by
  \(34.54\) mV while \(B^+\) stays quiet.  Both signed bias states hold after
  \(pacc\) closes.  This closes the local-bias gap in the device ladder: the
  same stored/restored error rail family can update weights using
  activation-error products and update biases using the error rail alone.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-bias-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_cell_update_ngspice.png}
  \caption{Ngspice integrated one-sample local-feature update characterization
  for the hybrid restored writer.  This deck combines the pieces that were
  isolated in the preceding plots: MOS hidden-error sign storage/restoration,
  MOS activation-gate sampling, an activation-gated product write into
  \(W^+/W^-\), a bias write into \(B^+/B^-\), and continuous transistor
  readback from both persistent capacitor pairs.  For the tested \(a^+e^+\)
  quadrant, the circuit implements \(\Delta W^+\propto a^+e^+\),
  \(\Delta W^-\approx0\), \(\Delta B^+\propto e^+\), and
  \(\Delta B^-\approx0\).  The stored hidden-error differential is \(98.5\) mV; the selected and
  complementary restored error gates are \(0.196\) V and \(1.731\) V; the
  sampled active activation gate is \(0.920\) V.  The same \(pacc\) pulse
  writes \(W^+-W^-=22.99\) mV and \(B^+-B^-=34.54\) mV, while the inactive
  \(W^-\) and \(B^-\) rails move only about \(0.002\,\mu\)V.  NMOS readback
  slices convert the written weight and bias states into \(19.06\,\mu\)A and
  \(29.17\,\mu\)A current steps, and both capacitor differentials hold after
  the update pulse to the plotted precision.  This is still a one-quadrant
  component proof, but it closes the first full local-feature update/readback
  loop without behavioral update buckets.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-cell-update-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_cell_quadrants_ngspice.png}
  \caption{Ngspice four-quadrant integrated local-feature update/readback
  characterization for the hybrid restored writer.  This is the family
  completion of Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-cell-update-ngspice}:
  every matched copy samples \(a^+\) or \(a^-\), uses the stored/restored
  \(e^+\) or \(e^-\) rails, writes both persistent weight and bias capacitor
  pairs, and reads both states back through NMOS synapse slices.  The measured
  weight differentials for \(a^+e^+\), \(a^+e^-\), \(a^-e^+\), and \(a^-e^-\)
  are \(+22.99\), \(-22.99\), \(-22.99\), and \(+22.99\) mV, matching
  \(\Delta W\propto ae\).  The measured bias differentials are \(+34.54\),
  \(-34.54\), \(+34.54\), and \(-34.54\) mV, matching
  \(\Delta B\propto e\) independent of the activation sign.  Readback signs
  match the stored differentials: weight current steps are \(+19.06\),
  \(-19.06\), \(-19.06\), and \(+19.06\,\mu\)A, while bias current steps are
  \(+29.17\), \(-29.17\), \(+29.17\), and \(-29.17\,\mu\)A.  This does not
  yet make a full transistor MNIST cell, but it closes the signed local-update
  routing contract for the weight and local-bias state roles without
  behavioral update sources.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-cell-quadrants-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_ngspice.png}
  \caption{Ngspice update-to-forward-store characterization for the hybrid
  local-feature cell.  This MOS-only deck performs one positive
  \(a^+e^+\) sample update, then uses the same persistent \(W^+/W^-\) and
  \(B^+/B^-\) capacitor states to drive a shared signed summing pair and a
  crossed MOS forward pair.  The experimentally checked contract is
  \(\Delta(W^+-W^-)>0\), \(\Delta(B^+-B^-)>0\),
  \(z^- - z^+ =
  F_{\mathrm{read}}(W^+-W^-,B^+-B^-;x^+-x^-)>0\), and
  \(h^- - h^+ = F_{\mathrm{fwd}}(z^- - z^+) > 0\), with the final
  \(h^- - h^+\) value stored on activation capacitors through
  a MOS pass switch.  The measured write step is
  \(W^+-W^-=22.99\) mV and \(B^+-B^-=34.54\) mV.  During the read phase those
  states create a \(52.07\) mV summed preactivation and a forward-pair load
  that peaks at \(73.34\) mV; the activation store captures and holds
  \(47.35\) mV after \(pact\) closes.  The read/forward phases disturb the
  weight and bias states by only \(0.005\,\mu\)V and \(0.002\,\mu\)V,
  respectively.  The important sizing lesson is that the activation-store
  phase \(pact\) must close inside the valid forward-load window; otherwise the store
  tracks the later common-mode droop.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_quadrants_ngspice.png}
  \caption{Ngspice four-quadrant update-to-forward-store characterization for
  the hybrid local-feature cell.  This family-completion deck extends
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-ngspice}
  from \(a^+e^+\) to \(a^+e^+\), \(a^+e^-\), \(a^-e^+\), and \(a^-e^-\).
  Each matched copy stores the same \(e^+\) and \(e^-\) rails, samples either
  the \(a^+\) or \(a^-\) activation gate, writes persistent \(W^+/W^-\) and
  \(B^+/B^-\), reads those states into a signed summing pair, and stores the
  crossed-MOS forward-pair output on \(h^+/h^-\).  The experimentally checked
  equations are \(\Delta W \propto ae\), \(\Delta B\propto e\), and, when the
  next read input has the same sign as the sampled activation,
  \(z^- - z^+ \propto \Delta W\,a+\Delta B\), so the stored activation follows
  the error sign.  The measured \(W^+-W^-\) steps are \(+22.99\), \(-22.99\),
  \(-22.99\), and \(+22.99\) mV; the measured \(B^+-B^-\) steps are
  \(+34.54\), \(-34.54\), \(+34.54\), and \(-34.54\) mV.  The summed
  preactivations are \(+50.76\), \(-50.76\), \(+50.76\), and \(-50.76\) mV,
  and the held activations are \(+47.35\), \(-47.35\), \(+47.35\), and
  \(-47.35\) mV.  This closes the quadrant gap left by the one-sample
  update-forward plot: signed local writes now round-trip through the MOS
  forward path with the expected sign in every activation/error quadrant.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-quadrants-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_reuse_ngspice.png}
  \caption{Ngspice same-cell reuse characterization for the hybrid
  update-to-forward path.  Unlike the matched-copy quadrant plots, this deck
  reuses one physical hidden-error store, one activation sample capacitor, one
  persistent \(W^+/W^-\) and \(B^+/B^-\) state, one summing pair, and one
  forward-store pair across two samples.  The first sample stores \(e^+\) and
  \(a^+\), writes \(W^+-W^-=22.99\) mV and \(B^+-B^-=34.54\) mV, reads a
  \(52.07\) mV preactivation, and stores \(47.35\) mV of activation.  A MOS
  transmission-gate reset then clears only the transient hidden-error,
  activation-input, summing, and forward-store capacitors; the persistent
  \(W/B\) rails remain untouched.  The reset devices are sized
  \(W_N=60\,\mu\mathrm{m}\), \(W_P=180\,\mu\mathrm{m}\) in this integrated
  reuse deck; the earlier \(24/60\,\mu\mathrm{m}\) reset left millivolt-scale
  residue on the large summing capacitor.  The second sample stores \(e^-\) with the
  same \(a^+\) activation and locally writes the opposite rails, cancelling the
  persistent differentials to the plotted precision.  After cancellation the
  individual rails are both elevated
  (\(W^+=W^-\approx0.873\) V and \(B^+=B^-\approx0.885\) V), so the zero is a
  differential cancellation rather than a hidden reset of the trainable state.
  The residual summed preactivation is about \(0.013\) mV and the second stored
  activation is about \(0.011\) mV, showing that the previous residue was a
  transient-state reset sizing issue rather than hidden cancellation of the
  trainable rails.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_reset_strength_ngspice.png}
  \caption{Ngspice integrated reset-strength sweep for the same update-to-forward
  reuse deck.  The sweep reruns the full two-sample circuit while changing only
  the transient-state reset devices.  The old \(W_N/W_P=24/60\,\mu\mathrm{m}\)
  sizing leaves \(2.46\) mV of cancelled-state preactivation residue and
  \(1.37\) mV of stored activation residue.  Increasing the reset pair to
  \(60/180\,\mu\mathrm{m}\) reduces those residues to \(0.013\) mV and
  \(0.011\) mV, respectively, below the plotted \(0.02\) mV target, while the
  first valid write/read/store response remains fixed at about \(50.76\) mV
  preactivation and \(47.35\) mV stored activation for every reset size.  This
  makes the reset-sizing tradeoff explicit instead of hiding it inside the
  reuse waveform.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-reset-strength-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_reuse_ngspice.png}
  \caption{Ngspice read-reuse characterization for the integrated MOS
  update-to-forward path.  A single \(a^+e^+\) local update first writes
  persistent \(W^+/W^-\) and \(B^+/B^-\) capacitor pairs; after that write,
  the circuit performs three read/reset/forward-store cycles without any
  second update and without forcing the trainable rails.  The stored state
  after the write is \(W^+-W^-=22.988\) mV and \(B^+-B^-=34.543\) mV.
  Repeated MOS reads reproduce \(z^- - z^+=50.762, 51.164, 50.775\) mV, and
  repeated forward-store pulses produce \(h^- - h^+=47.346, 47.325,
  47.326\) mV.  The reset transmission gates clear only the transient
  summing and activation-storage capacitors between reads, leaving reset
  samples of about \(0.013\) mV on \(z\) and \(0.000\) mV on \(h\).  The final
  trainable-state drift is only \(0.003\,\mu\mathrm{V}\) on \(W\) and
  \(0.002\,\mu\mathrm{V}\) on \(B\), so the same MOS-written state can be
  reused by forward inference reads without measurable read disturb in this
  operating window.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_signed_read_reuse_ngspice.png}
  \caption{Ngspice signed read-reuse characterization for the integrated MOS
  update-to-forward path.  This is the sign-family completion of
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-reuse-ngspice}:
  one deck writes a positive \(W/B\) state from \(a^+e^+\), while a mirrored
  deck writes a negative \(W/B\) state from \(a^+e^-\).  No second update is
  applied; both decks repeatedly read, reset only the transient summing and
  activation capacitors, and store the forward output from the same persistent
  trainable rails.  The negative deck writes \(W^+-W^-=-22.988\) mV and
  \(B^+-B^-=-34.543\) mV, then reproduces \(z^- - z^+=-50.762,-51.164,
  -50.775\) mV and \(h^- - h^+=-47.346,-47.325,-47.326\) mV across the three
  read/store cycles.  These values mirror the positive deck to the plotted
  precision, while both signs reset \(z/h\) between reads to about
  \(0.013/0.000\) mV and disturb the persistent W/B state by only about
  \(0.003/0.002\,\mu\mathrm{V}\).  This closes the remaining same-physical-cell
  sign gap for update-to-forward reuse: both signed trainable states can drive
  repeated forward reads without behavioral readback or capacitor forcing.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-signed-read-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_pact_timing_ngspice.png}
  \caption{Ngspice read-to-\(pact\) timing characterization for the integrated
  MOS update-to-forward path.  Each variant first performs the same positive
  local update and read setup as
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-reuse-ngspice},
  then changes only the activation-store pulse timing.  A pre-read \(pact\)
  stores \(0.000\) mV because no summing state has been produced.  A read-edge
  \(pact\) stores a partial \(32.969\) mV while the summing state is still only
  \(z^- - z^+=32.764\) mV.  The settled timing stores \(47.346\) mV from a
  \(52.066\) mV preactivation.  A late short pulse sees the same valid
  \(52.066\) mV preactivation but stores only \(20.806\) mV because the
  activation capacitor does not have enough aperture before reset.  After the
  reset pulse clears the transient state, \(pact\) stores only \(0.018\) mV.
  The resulting circuit rule is stricter than ``sample sometime after read'':
  \(pact\) must overlap a settled read state for long enough before reset.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-pact-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_pact_aperture_ngspice.png}
  \caption{Ngspice activation-store aperture-width characterization for the
  integrated MOS update-to-forward path.  Each deck uses the same positive
  MOS-written \(W/B\) state and the same read phase, then sweeps only the
  \(pact\) high-time.  The measured stored activations for
  \(40,80,120,160,240,320\) ns are \(38.289,46.704,47.858,43.543,31.693,
  32.925\) mV.  The preactivation is already near \(52.066\) mV for every
  case after the shortest one, so the non-monotone response is not a missing
  read signal.  Short apertures undercharge the activation capacitor; long
  apertures keep the capacitor connected after the forward load has started to
  droop.  In this sizing and phase schedule, the useful window is centered
  near \(80\)--\(120\) ns rather than being improved by simply holding
  \(pact\) high longer.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-pact-aperture-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_pact_store_topology_ngspice.png}
  \caption{Ngspice activation-store topology comparison for the integrated MOS
  update-to-forward path.  The baseline uses the NMOS-only \(pact\) sampling
  switches from Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-pact-aperture-ngspice}.
  The transmission-gate variant adds parallel PMOS pass devices
  (\(W_P=120\,\mu\mathrm{m}\)) driven by complementary \(pact_n\), with all
  update, read, reset, and storage capacitors otherwise unchanged.  For
  \(40,80,120,160,240,320\) ns apertures, the NMOS-only store captures
  \(38.289,46.704,47.858,43.543,31.693,32.925\) mV, while the TG store
  captures \(46.588,52.124,52.141,46.246,34.668,37.163\) mV.  The TG store is
  therefore the better physical sampling topology for short apertures and can
  capture the full differential near \(80\)--\(120\) ns.  It does not remove
  the long-aperture failure mode: once \(pact\) is held through the forward
  load droop, the activation capacitor still tracks downward.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-pact-store-topology-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_gated_store_ngspice.png}
  \caption{Ngspice read-gated activation-store characterization for the
  integrated MOS update-to-forward path.  The read-gated variant inserts a
  second series transmission gate controlled by a read-valid phase, so a long
  \(pact\) pulse cannot keep the activation capacitor connected indefinitely
  after the read path turns off.  Raw read-gating improves the long apertures
  but still samples some forward-load collapse:
  \(46.634,52.090,52.117,46.266,40.625,40.625\) mV for
  \(40,80,120,160,240,320\) ns.  A guarded non-overlap valid phase, ending at
  the experimentally valid \(pact\) window, captures
  \(46.577,52.075,52.111,51.355,51.355,51.355\) mV over the same sweep.  The
  transistor-level lesson is that the store switch should be a
  transmission-gate stack gated by both \(pact\) and a non-overlap read-valid
  clock; sizing the pass devices alone does not fix a phase-validity error.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-gated-store-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_timing_ngspice.png}
  \caption{Ngspice guard-off timing sweep for the same MOS integrated
  update-to-forward activation store.  The \(pact\) aperture is deliberately
  held long at \(320\) ns, while the non-overlap guard is ended after
  \(60,100,130,150,180,\) or \(220\) ns.  The read preactivation remains
  valid at about \(52.066\) mV in all cases, but the sampled activation is
  \(50.275,52.654,51.355,48.570,40.624,\) and \(34.940\) mV.  This separates
  the two timing requirements: the guard only needs to stay open long enough
  to charge the activation capacitor, and it must close before the forward
  read load collapses.  In this Level-1 sizing, the robust window is roughly
  \(100\)--\(130\) ns rather than the full read pulse.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_skew_ngspice.png}
  \caption{Ngspice guard-clock skew sweep for the integrated MOS activation
  store.  The \(pact\) aperture is held long at \(320\) ns, and the
  guard-valid pulse width is fixed at \(130\) ns.  Shifting that guard window
  by \(-120,-80,-40,0,+40,+80,+120\) ns stores
  \(32.260,48.780,52.517,51.355,42.401,32.238,\) and \(28.899\) mV while the
  read preactivation remains near \(52.066\) mV.  The skew window is
  asymmetric: early guard skew is acceptable until the overlap with \(pact\)
  becomes too short, but late guard skew samples the forward-load droop almost
  immediately.  The clocking rule for this store is therefore to bias the
  valid window early and close it before the read path collapses.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-skew-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_edge_ngspice.png}
  \caption{Ngspice guard-edge slew sweep for the integrated MOS activation
  store.  The \(pact\) aperture is held long at \(320\) ns, the guard-valid
  plateau remains \(3.18\)--\(3.31\,\mu\mathrm{s}\), and only the linear
  guard rise/fall time changes: \(5,10,20,40,80,\) or \(120\) ns.  The read
  preactivation remains \(52.066\) mV, while the stored activation is
  \(51.536,52.415,51.355,50.611,49.346,\) and \(46.358\) mV.  This sweep
  shows that ordinary tens-of-ns control slew is not the main failure mode;
  loss appears only when the falling edge becomes so slow that the partially
  enabled guard samples into the read-path collapse.  The clock design rule is
  therefore about absolute valid-window placement first, and edge rate second.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-edge-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_pact_edge_ngspice.png}
  \caption{Ngspice \(pact\)-edge slew sweep for the guarded MOS activation
  store.  The read and guard windows are fixed at their nominal timings, while
  the \(pact\) rise begins at \(3.16\,\mu\mathrm{s}\) and the rise/fall time
  is swept through \(5,10,20,40,80,\) and \(120\) ns.  The read
  preactivation remains \(52.066\) mV, while the stored activation is
  \(51.378,51.352,51.355,50.934,48.721,\) and \(43.040\) mV.  This effect
  is weaker than the guard-window placement errors, but it is not zero:
  ordinary \(5\)--\(20\) ns \(pact\) edges agree to tens of microvolts, a
  \(40\) ns edge loses only \(0.42\) mV, and a \(120\) ns edge loses about
  \(8.31\) mV because \(pact\) is still slewing during much of the valid
  guard window.  The series stack therefore needs both correct guard timing
  and a reasonably settled \(pact\) gate at guard open.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-pact-edge-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_start_ngspice.png}
  \caption{Ngspice guard-start timing sweep for the integrated MOS
  activation store.  The guard fall is fixed at \(3.31\,\mu\mathrm{s}\), the
  \(pact\) aperture is held long at \(320\) ns, and only the guard rise end is
  moved through \(3.10,3.14,3.18,3.22,3.26,\) and
  \(3.29\,\mu\mathrm{s}\).  The read preactivation remains \(52.066\) mV,
  while the stored activation is
  \(51.359,51.359,51.355,49.183,40.449,\) and \(26.159\) mV.  This isolates
  the start-side requirement from the close-side droop in the skew sweep:
  opening the guard before \(pact\) is harmless because the series \(pact\)
  transmission gate still blocks the store, but opening after the nominal
  point directly cuts charge time and undercharges the activation capacitor.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-start-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_fall_ngspice.png}
  \caption{Ngspice read-valid fall sweep for the guarded MOS activation
  store.  The guard remains fixed at \(3.18\)--\(3.31\,\mu\mathrm{s}\), with
  a \(20\) ns fall, while the read-control fall starts at
  \(3.26,3.30,3.33,3.36,3.40,\) or \(3.44\,\mu\mathrm{s}\).  The stored
  activation is \(57.604,51.926,51.355,51.355,51.355,\) and \(51.355\) mV.
  The surprising case is the earliest read fall: instead of undercharging,
  the activation store oversamples a read-turnoff transient while the guard is
  still open.  Once the guard has disconnected the activation capacitor,
  later read fall timing no longer changes the stored value.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-fall-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_read_rise_ngspice.png}
  \caption{Ngspice read-valid rise sweep for the guarded MOS activation
  store.  The guard remains fixed at \(3.18\)--\(3.31\,\mu\mathrm{s}\), the
  read fall starts at \(3.36\,\mu\mathrm{s}\), and the read-control rise ends
  at \(2.72,3.00,3.10,3.18,3.24,\) or \(3.28\,\mu\mathrm{s}\).  The stored
  activation is \(51.355,34.123,23.346,14.559,7.923,\) and \(3.489\) mV.
  Unlike the read-fall side, the read-rise side is monotone: delaying read
  validity reduces the preactivation available during the guard window and
  directly undercharges the activation store.  The guard should therefore
  open only after the read path has had enough time to settle.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-read-rise-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_hold_ngspice.png}
  \caption{Ngspice no-reset hold check for the guarded MOS activation store.
  The same nominal guard window and long \(pact\) aperture are used, but the
  reset pulse after capture is disabled so the activation capacitor must hold
  until the end of the transient.  The stored activation is
  \(51.35455,51.35444,51.35431,51.35419,\) and \(51.35407\) mV at
  \(3.575,4.50,5.50,6.50,\) and \(7.45\,\mu\mathrm{s}\), for a total drift of
  about \(-0.48\,\mu\mathrm{V}\).  The preactivation remains near
  \(52.066\) mV.  In this Level-1 model and leakage setup, the guarded store
  is limited by timing/skew rather than immediate hold leakage.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-hold-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_signed_hold_ngspice.png}
  \caption{Ngspice signed hold check for the guarded MOS activation store.
  The positive deck writes the usual \(W^+/B^+\) state, while the negative
  deck uses the existing gate-swapped update path to write \(W^-/B^-\).  Both
  are then routed through the same \(pact\)-and-guard transmission-gate stack,
  with reset disabled after capture.  The preactivation samples are exact
  mirrors to the reported precision:
  \(+52.065847,+52.065845,+52.065842,+52.065840,+52.065838\) mV and the
  corresponding negatives.  The stored activations are likewise mirrored:
  \(+51.354550,+51.354440,+51.354310,+51.354190,+51.354070\) mV and
  \(-51.354550,-51.354440,-51.354310,-51.354190,-51.354070\) mV at
  \(3.575,4.50,5.50,6.50,\) and \(7.45\,\mu\mathrm{s}\).  The mirror error
  is \(0\) mV in the saved samples.  This separates sign correctness from the
  previous positive-only guard timing checks: the guard stack does not create
  a one-sided activation-store artifact in this Level-1 differential model.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-signed-hold-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_off_isolation_ngspice.png}
  \caption{Ngspice off-isolation stress for the guarded MOS activation store.
  After one nominal capture, reset is disabled and the deck deliberately
  toggles later read pulses, \(pact\)-only pulses with the guard off, and
  guard-only/read+guard pulses with \(pact\) off.  The preactivation node is
  not quiet under this stress: sampled \(z^- - z^+\) values move from
  \(52.066\) mV to \(64.749,69.981,69.981,53.486,20.843,\) and
  \(6.105\) mV as later read/control conditions change.  The stored
  activation remains isolated, however: sampled \(h^- - h^+\) is
  \(51.35455,51.35444,51.35437,51.35431,51.35422,51.35415,\) and
  \(51.35406\) mV at \(3.575,4.50,5.05,5.50,6.25,6.75,\) and
  \(7.45\,\mu\mathrm{s}\), for only \(-0.49\,\mu\mathrm{V}\) total drift.
  This closes a separate robustness condition from static hold: later control
  activity can disturb the summing/read side, but the series \(pact\)-and-guard
  transmission-gate stack keeps the activation capacitor disconnected.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-off-isolation-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_corner_ngspice.png}
  \caption{Ngspice guard pass-gate threshold-corner sweep for the same
  integrated MOS activation store.  The \(pact\) aperture is held long at
  \(320\) ns, and the guard uses the nominal \(130\) ns valid window from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-timing-ngspice}.
  Only the second, guard-controlled transmission gate changes threshold model:
  strong, nominal, N-weak, P-weak, and both-weak.  The read preactivation is
  \(52.066\) mV in all cases, and the stored activation is
  \(51.884,51.355,51.361,51.266,\) and \(51.274\) mV.  Correctly targeting
  the guard-controlled devices shows a small but real threshold sensitivity:
  the span is about \(0.62\) mV, dominated by the strong and P-weak cases,
  rather than the nearly flat result obtained when the wrong series switch was
  swept.  The dominant robustness risk is still clock skew/overlap, but the
  guard TG should be treated as a real sampled-data switch with finite corner
  sensitivity.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-corner-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_size_ngspice.png}
  \caption{Ngspice guard transmission-gate width sweep for the corrected
  guard-controlled switch in the MOS activation store.  The nominal device
  pair is \(96\,\mu\mathrm{m}/240\,\mu\mathrm{m}\) for NMOS/PMOS; the sweep
  uses \(0.125\times,0.25\times,0.5\times,1.0\times,\) and \(1.5\times\)
  scaling, corresponding to \(12/30,24/60,48/120,96/240,\) and
  \(144/360\,\mu\mathrm{m}\).  The read preactivation remains
  \(52.066\) mV and the stored activation is
  \(51.078,51.255,51.425,51.355,\) and \(51.365\) mV.  Even the smallest
  tested guard switch captures the signed activation, so the current topology
  is not width-limited at the nominal point; timing closure remains the larger
  design constraint.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-size-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_cstore_ngspice.png}
  \caption{Ngspice activation-store capacitance sweep for the guarded MOS
  store.  The read, \(pact\), and guard timings are fixed at the nominal
  guarded-store setting, and only \(C_\mathrm{store}\) changes:
  \(2,5,10,20,50,\) and \(100\) pF.  The read preactivation stays near
  \(52.066\) mV, while the stored activation is
  \(61.070,56.111,51.355,43.865,30.215,\) and \(20.513\) mV.  The tradeoff
  is two-sided.  Very small activation capacitors charge quickly but expose
  clock/switch feedthrough as an overshoot above the read preactivation; very
  large capacitors suppress that feedthrough but undercharge in the fixed
  \(130\) ns guard window.  The nominal \(10\) pF choice sits near the useful
  middle for this Level-1 sizing, so capacitance is a real part of the
  timing/noise budget rather than a free hold knob.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-cstore-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_control_swing_ngspice.png}
  \caption{Ngspice \(pact\)-and-guard control-swing sweep for the guarded MOS
  activation store.  The read path, \(C_\mathrm{store}=10\) pF, and nominal
  timing are fixed; only the two series transmission-gate control swings are
  reduced together.  The complementary PMOS gate low level is raised by the
  same amount that the NMOS gate high level is lowered, so a \(1.2\) V swing
  means \(pact/guard\) high at \(1.2\) V and \(pact_n/guard_n\) low at
  \(0.6\) V.  The read preactivation remains near \(52.066\) mV for every
  case, but the stored activation is
  \(51.355,51.336,51.782,58.088,0.000,\) and \(0.000\) mV for
  \(1.8,1.6,1.4,1.2,1.0,\) and \(0.8\) V swing.  Thus the current guarded
  store has comfortable margin down to roughly \(1.4\) V control swing; the
  \(1.2\) V case is not a clean analog weakening but a marginal pass-stack
  regime with feedthrough/settling overshoot, and \(1.0\) V no longer turns
  the stack on enough to sample the activation.  The robust design rule is
  that phase-driver headroom is a real circuit constraint, not merely a logic
  convention.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-control-swing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_read_drive_ngspice.png}
  \caption{Ngspice read-valid drive sweep for the guarded MOS activation store.
  The persistent \(W/B\) state, \(C_\mathrm{store}=10\) pF, \(pact\) window,
  and guard window are fixed; only the NMOS read-gate drive voltage changes.
  The measured preactivations are
  \(6.655,22.013,41.643,52.066,59.820,\) and \(62.056\) mV for
  \(0.75,0.90,1.05,1.15,1.30,\) and \(1.45\) V read drive, while the stored
  activations are
  \(8.428,26.991,44.247,51.355,56.009,\) and \(56.687\) mV.  This separates
  read-path headroom from store-switch headroom.  A weak read gate does not
  primarily fail because the activation capacitor cannot hold; it fails
  earlier because the read tail current cannot build enough preactivation
  before the guard closes.  Increasing read drive above the nominal \(1.15\) V
  gives additional stored activation, but the benefit saturates near the
  mid-\(50\) mV range under the nominal forward-pair gain.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-read-drive-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_read_width_ngspice.png}
  \caption{Ngspice read-tail width sweep for the guarded MOS activation store.
  This is the device-sizing counterpart to
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-read-drive-ngspice}:
  the read-valid voltage remains at the nominal \(1.15\) V, while only the
  NMOS read-tail width \(W_\mathrm{read}\) changes.  For
  \(W_\mathrm{read}=6,12,24,48,96,\) and \(192\,\mu\mathrm{m}\), the measured
  preactivations are
  \(17.393,33.678,52.066,61.495,62.753,\) and \(62.898\) mV, and the stored
  activations are
  \(21.209,37.538,51.355,57.040,57.355,\) and \(57.202\) mV.  The sizing
  result matches the drive-voltage result: undersized read devices limit the
  summing/forward signal before the guard closes, while widths above roughly
  \(48\)--\(96\,\mu\mathrm{m}\) mostly hit the read-preactivation ceiling under
  the nominal forward-pair gain.  The nominal \(24\,\mu\mathrm{m}\) read tail
  is functional but not heavily overdesigned; a larger read tail buys several
  millivolts of capture margin before diminishing returns dominate.  The next
  forward-pair sweep shows that this is not a hard activation-store ceiling.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-read-width-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_ngspice.png}
  \caption{Ngspice forward-pair/tail width sweep for the guarded MOS activation
  store.  This sweep keeps the same MOS-written \(W/B\) state, read-valid
  drive, read-tail sizing, \(pact\), guard timing, and \(C_\mathrm{store}=10\)
  pF as the nominal guarded readback case.  Only the three NMOS devices in the
  crossed source-coupled forward pair and its tail are resized together.  The
  measured read preactivation is essentially unchanged at \(52.066\) mV for
  every case, proving that the sweep isolates the forward readout rather than
  perturbing the stored summing capacitors.  For forward-pair/tail widths of
  \(6,12,24,48,96,\) and \(192\,\mu\mathrm{m}\), the immediate forward-load
  differentials are
  \(12.318,21.020,33.618,51.130,75.728,\) and \(110.931\) mV, and the guarded
  stored activations are
  \(12.479,21.184,33.794,51.355,76.077,\) and \(111.674\) mV.  This corrects
  the earlier interpretation of the read-width plateau: the current nominal
  forward pair is a gain setting, not a saturated store limit.  If larger
  activation swing is useful for the learning cell, the next design knob is
  forward-pair/tail sizing or bias, followed by renewed mismatch, noise, and
  common-mode checks at the chosen gain.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_robustness_ngspice.png}
  \caption{Ngspice robustness check for the \(96\,\mu\mathrm{m}\) forward-pair
  candidate from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-ngspice}.
  The deck keeps the same MOS-written \(W/B\) state, read path, \(pact\),
  guard timing, and \(10\) pF activation store, but repeats the signed hold
  and off-isolation checks with the larger forward pair.  The positive
  \(96\,\mu\mathrm{m}\) stored activation samples are
  \(76.07747,76.07731,76.07713,76.07694,\) and \(76.07677\) mV at
  \(3.575,4.50,5.50,6.50,\) and \(7.45\,\mu\mathrm{s}\), while the negative
  case mirrors them exactly to the saved precision.  The read preactivation
  remains \(\pm52.06585\) mV, so the larger stored activation comes from
  forward-pair gain, not a changed summing state.  In the off-isolation stress
  deck, later read and phase-control activity moves the preactivation side
  through \(52.066,64.749,69.981,69.981,53.486,20.843,\) and \(6.105\) mV,
  but the held \(96\,\mu\mathrm{m}\) activation drifts only
  \(0,-0.16,-0.27,-0.35,-0.49,-0.59,\) and \(-0.71\,\mu\mathrm{V}\).  This
  makes \(96\,\mu\mathrm{m}\) a credible next gain candidate for further
  mismatch/noise/common-mode checks rather than only a nominal-width plot.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-robustness-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_zcm_ngspice.png}
  \caption{Ngspice summing-common-mode headroom sweep for the
  \(96\,\mu\mathrm{m}\) forward-pair candidate.  The deck keeps the same
  MOS-written \(W/B\) state, read path, guard timing, \(10\) pF activation
  store, and \(96\,\mu\mathrm{m}\) crossed forward pair, but resets both
  summing capacitors to \(0.75,0.85,0.90,0.95,\) and \(1.05\) V.  The read
  preactivation remains positive in every case:
  \(47.585,51.453,52.066,52.222,\) and \(52.297\) mV.  The stored activation
  does not follow that preactivation at low common mode, however: the measured
  stored values are \(0,27.139,76.077,88.630,\) and \(92.774\) mV.  Thus the
  larger forward pair is not generally headroom-robust; it needs roughly the
  nominal \(0.90\) V summing common mode or higher in this Level-1 fixture.
  This turns common-mode generation/reset into a real circuit requirement
  before the high-gain candidate can be promoted to mismatch/noise tests.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-zcm-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.90\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_tail_rebias_ngspice.png}
  \caption{Ngspice private forward-tail rebias sweep for the same
  \(96\,\mu\mathrm{m}\) forward-pair common-mode failure.  This is a negative
  design result: the deck keeps the same MOS-written state and \(96\,\mu\mathrm{m}\)
  forward pair, resets the summing capacitors to \(0.75,0.85,\) or \(0.90\) V,
  and replaces only the crossed forward-pair tail gate with a private
  \(0.55,0.65,0.75,0.85,\) or \(0.95\) V bias.  The read preactivation remains
  real in every case, but tail rebias does not solve the low-common-mode
  headroom problem.  At \(V_{ZCM}=0.75\) V, the stored activations are
  \(0,0,0.00001,0.00001,\) and \(0\) mV.  At \(V_{ZCM}=0.85\) V, they are
  \(0,24.957,26.456,26.921,\) and \(27.139\) mV: still a partial,
  underdriven activation.  At nominal \(V_{ZCM}=0.90\) V, the same tail sweep
  gives \(0,72.284,74.965,75.729,\) and \(76.077\) mV, so an excessively low
  tail bias can also kill an otherwise valid nominal-common-mode readout.
  The high-gain path therefore needs a summing-common-mode or input-headroom
  fix, not merely a forward-tail-bias trim.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-tail-rebias-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_ngspice.png}
  \caption{Ngspice passive shifted-gate-latch headroom check for the
  \(96\,\mu\mathrm{m}\) forward-pair candidate.  This deck keeps the MOS-written
  \(W/B\) state, read path, guard timing, and \(10\) pF activation store fixed,
  but stops driving the large crossed forward-pair gates directly from the
  summing capacitors.  Instead, two \(5\) pF gate-storage capacitors are reset
  to \(0.90\) V and the signed summing nodes are AC-coupled into them before
  the crossed pair.  At \(V_{ZCM}=0.75\) V the unshifted path stores no useful
  activation, even though the read preactivation is about \(47.6\) mV.  The
  shifted latch restores a real stored activation: for \(5,10,20,\) and
  \(50\) pF coupling into the \(5\) pF gate latch, the measured stored values
  are \(43.245,52.154,13.414,\) and \(0.091\) mV.  The useful coupling is
  therefore a window, not monotone gain.  With the selected \(10\) pF coupling,
  \(V_{ZCM}=0.75,0.85,\) and \(0.90\) V store \(52.154,60.215,\) and
  \(61.115\) mV, and the negative \(0.75\) V case mirrors at \(-52.154\) mV
  with sub-\(0.3\) mV hold drift.  This is the first real low-common-mode
  headroom fix candidate for the high-gain forward pair, but it still needs
  reset-feedthrough, mismatch, noise, and repeated/off-isolation checks before
  it can be promoted beyond this Level-1 fixture.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_off_isolation_ngspice.png}
  \caption{Ngspice off-isolation stress for the shifted-gate headroom fix.  The
  deck uses the selected \(10\) pF coupling into \(5\) pF shifted gate-storage
  capacitors, starts from the low \(V_{ZCM}=0.75\) V summing-common-mode case,
  and then applies later read-only, \(pact\)-only, guard-only, and
  guard-plus-read-only activity after the first activation capture.  The
  shifted gate latch captures \(52.153750\) mV and the later stored samples are
  \(52.153640,52.153560,52.153510,52.153410,52.153350,\) and
  \(52.153260\) mV, for only about \(-0.49\,\mu\mathrm{V}\) total drift.  The
  preactivation side is intentionally disturbed during the stress
  (\(47.446\rightarrow52.972\rightarrow44.189\rightarrow26.215
  \rightarrow3.263\rightarrow0.166\) mV), so this plot is testing isolation of
  the stored activation, not quiet hold.  The shifted-gate topology therefore
  passes the same basic later-control-activity check as the unshifted guarded
  store.  At this point the remaining promotion gaps are reset/feedthrough,
  mismatch/noise, and repeated-cycle behavior.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-off-isolation-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_feedthrough_ngspice.png}
  \caption{Ngspice physical reset/feedthrough stress for the shifted-gate
  latch.  The previous shifted-gate characterization initialized the gate
  storage capacitors directly, so this deck deliberately starts them from a
  bad differential state (\(V_{zpg}=0.35\) V and \(V_{zmg}=1.45\) V) and
  compares no-reset behavior with a real reset pulse through the MOS reset
  devices.  Without the reset pulse, the bad gate state remains about
  \(-371.5\) mV at the read edge and the activation store overdrives to
  \(362.3\) mV.  With the physical reset pulse, the gate differential is
  cleared to below \(0.118\) mV before read, the common mode is restored to
  \(0.899993\) V, and the final stored activation is \(57.6\) mV.  The reset
  case is not identical to the nominal initialized shifted-gate result
  (\(52.2\) mV), so reset charge/feedthrough remains a sizing concern, but
  the reset devices remove the bad-initial-state overdrive path and preserve a
  useful low-common-mode activation.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-feedthrough-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_repeated_reset_reuse_ngspice.png}
  \caption{Ngspice repeated physical reset/read/store reuse check for the
  shifted-gate headroom fix.  This deck starts from one MOS-written \(W/B\)
  state at \(V_{ZCM}=0.75\) V, uses the selected \(10\) pF coupling into
  \(5\) pF shifted-gate capacitors, and runs three read/forward-store cycles
  with real reset pulses between cycles.  The three read preactivation samples
  are \(46.625,46.823,\) and \(46.582\) mV; the stored activation samples are
  \(49.528,56.585,\) and \(56.585\) mV.  The reset pulses clear the shifted
  gate differential between reads to \(0.000466\) and \(0.000456\) mV, return
  the gate common mode to \(0.900003\) V, leave only about \(0.019\) mV of
  summing-node residue, and clear the activation store to numerical zero.
  The persistent trainable state is not read-disturbed: over the repeated
  cycles the \(W\) and \(B\) differentials drift by only \(0.003\) and
  \(0.002\,\mu\mathrm{V}\).  The later captures are higher than the first
  because the reset path adds a small repeatable charge/feedthrough offset, so
  reset sizing is still worth tuning, but the circuit now demonstrates useful
  repeated low-common-mode operation without Python capacitor forcing.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-repeated-reset-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_offset_noise_ngspice.png}
  \caption{Ngspice deterministic shifted-gate offset/noise sweep for the
  \(96\,\mu\mathrm{m}\) low-common-mode headroom fix.  The deck keeps the
  same MOS-written \(W/B\) state, \(V_{ZCM}=0.75\) V summing common mode,
  \(10\) pF coupling capacitors, and \(5\) pF shifted-gate capacitors, then
  perturbs the shifted-gate initial conditions before the read.  The nominal
  shifted-gate case stores \(52.154\) mV.  Common-mode offsets of
  \(-25\) and \(+25\) mV on both shifted gates store \(50.092\) and
  \(53.607\) mV, so common-mode gate noise is mild in this operating window.
  A helpful \(-25\) mV gate-differential residue increases the stored
  activation to \(66.787\) mV.  Destructive \(+25\) and \(+50\) mV
  gate-differential residues reduce the stored activation to \(37.274\) and
  \(22.219\) mV, respectively, without flipping the sign.  The practical rule
  is therefore clear: the shifted-gate latch is tolerant of modest
  common-mode residue, but differential gate residue directly consumes the
  recovered headroom margin and should be kept well below the useful coupled
  gate differential.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-offset-noise-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_threshold_ngspice.png}
  \caption{Ngspice input-pair threshold-skew characterization for the
  shifted-gate \(96\,\mu\mathrm{m}\) forward pair.  The deck keeps the same
  low-common-mode \(V_{ZCM}=0.75\) V read, \(10\) pF coupling capacitors,
  \(5\) pF shifted gates, and guarded activation store, but assigns separate
  Level-1 NMOS threshold models to the two crossed forward-pair input
  devices.  Matched strong/weak \(20\) mV corners keep the stored activation
  positive (\(54.942\) and \(45.662\) mV versus \(52.154\) mV nominal), while
  one-sided skew exposes the real offset budget.  Weakening the \(h^+\) side
  leaves only \(15.771\) mV, weakening the \(h^-\) side raises the store to
  \(81.091\) mV, and the two opposite \(20\) mV skews produce
  \(-19.373\) and \(116.936\) mV.  The passive shifted-gate differential is
  unchanged in the untrimmed cases, so this is not a latch sampling error; it
  is forward-pair threshold offset consuming the recovered headroom.  Helpful
  gate trim on the failing skew shows the required recovery scale:
  \(-25\) mV is still negative (\(-4.176\) mV), \(-50\) mV barely recovers
  positive sign (\(11.022\) mV), and \(-75\) mV gives \(26.162\) mV.  This
  plot blocks promoting the shifted-gate fix as a complete robust neuron
  element until the circuit adds per-cell offset trim/autozero, larger signal
  margin, or a less offset-sensitive forward pair.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-threshold-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_trimmed_reset_ngspice.png}
  \caption{Ngspice physical split-reset trim check for the failing
  shifted-gate threshold-skew corner.  This deck keeps the same
  \(V_{ZCM}=0.75\) V low-common-mode read, \(10\) pF coupling capacitors,
  \(5\) pF shifted gates, and \(+20/-20\) mV input-pair threshold skew that
  flipped the untrimmed activation in
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-threshold-ngspice}.
  The difference is that the shifted gates start from a deliberately bad
  initial state and are then reset through real MOS reset switches to split
  reference rails before the read.  The reset rails establish the requested
  pre-read gate differential while preserving the \(0.90\) V gate common mode:
  measured gate differentials for requested trims of
  \(0,-10,-15,-20,-25,-50,-75,\) and \(-100\) mV are
  \(0.000,-10.000,-15.000,-20.000,-24.999,-49.998,-74.998,\) and
  \(-99.997\) mV, with common mode \(0.899993\)--\(0.899994\) V.  The
  corresponding stored activations are
  \(-20.241,-0.766,8.970,18.705,28.438,77.060,125.552,\) and
  \(173.790\) mV.  Therefore the skew hazard is not fixed by reset alone:
  the untrimmed physical reset still flips sign.  But a hardware-realistic
  split reset/autozero rail does recover the sign around \(-15\) mV and gives
  useful margin by \(-25\) mV.  The large-trim cases intentionally show the
  other side of the budget: offset trim is a calibration knob, not a free
  gain knob, because excessive trim overdrives the stored activation.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-reset-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_balanced_trim_ngspice.png}
  \caption{Ngspice signed split-reset calibration check across both
  threshold-skew polarities.  This uses the same physical reset-switch deck as
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-reset-ngspice},
  but compares the nominal reset case, one-sided \(20\) mV threshold corners,
  and both opposite \(+20/-20\) mV skew signs.  The nominal physical-reset
  store is \(57.628\) mV.  Without trim, the one-sided \(h^+\)-weak and
  \(h^-\)-weak cases store \(18.653\) and \(96.295\) mV; signed trims of
  \(-20\) and \(+15\) mV move them back to \(57.510\) and \(67.220\) mV.
  The full skews are more severe: untrimmed \(+20/-20\) mV skew flips to
  \(-20.241\) mV, and untrimmed \(-20/+20\) mV skew overdrives to
  \(135.237\) mV.  Reversing the reset-trim polarity corrects both signs:
  \(-35\) mV trim stores \(47.897\) mV in the first skew, and \(+35\) mV trim
  stores \(67.355\) mV in the opposite skew.  This does not eliminate the
  need for calibration, but it shows the split-reset rail is a bidirectional
  offset-cancellation primitive rather than a one-corner workaround.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-balanced-trim-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_trimmed_reuse_ngspice.png}
  \caption{Ngspice calibrated repeated-reuse check for the skewed shifted-gate
  forward pair.  This is the same low-common-mode \(V_{ZCM}=0.75\) V deck,
  MOS-written weight/bias state, \(10\) pF shifted-gate coupling, and
  \(+20/-20\) mV forward-pair threshold skew that required split-reset
  calibration in
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-balanced-trim-ngspice}.
  The reset references are fixed at the calibrated \(-35\) mV split trim
  (\(V_{\mathrm{reset},+}=0.8825\) V and
  \(V_{\mathrm{reset},-}=0.9175\) V), and the cell is reused through three
  read/reset/store cycles without changing the stored \(W/B\) state.  The
  first read needs the same physical reset as later reads; otherwise it is
  still an untrimmed initial condition.  With that pre-reset in place, the
  reset samples re-establish shifted-gate differentials of
  \(-34.9986,-34.9995,\) and \(-34.9995\) mV while the reset common mode is
  \(0.899993,0.900003,\) and \(0.900003\) V.  Reset residue is small
  (\(|z|<0.064\) mV and \(h=0\)).  The three read samples are
  \(46.647,46.823,\) and \(46.582\) mV, and the stored activations are
  \(46.985,46.967,\) and \(46.967\) mV.  Weight and bias drift over the
  repeated use window are \(0.001\) and \(0.000\,\mu\mathrm{V}\).  This shows
  that the split-reset trim can be reused as a physical offset-cancellation
  operation in the Level-1 fixture; it does not yet solve how a larger array
  calibrates those trims or how much mismatch/noise margin remains.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_trimmed_margin_ngspice.png}
  \caption{Ngspice trim-margin sweep for the calibrated shifted-gate reuse
  fixture.  This uses the same \(+20/-20\) mV forward-pair threshold skew and
  repeated read/reset/store schedule as
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-reuse-ngspice},
  but sweeps the physical split-reset command around the selected \(-35\) mV
  point.  The reset switch network tracks the requested
  \(-25,-30,-35,-40,\) and \(-45\) mV differentials to within about
  \(0.002\) mV, while the reset common-mode samples remain at
  \(0.899993,0.900003,\) and \(0.900003\) V over the three cycles.  The read
  preactivation is almost unchanged (\(\approx 46.6\)--\(46.8\) mV) because
  this trim corrects the shifted forward-pair gate offset rather than the
  upstream summing node.  Stored activation is monotone and bounded over the
  window: the cycle means are
  \(27.722,37.349,46.973,56.590,\) and \(66.201\) mV, with per-trim cycle
  spread only \(0.0165\)--\(0.0197\) mV.  For this Level-1 fixture, the
  calibrated split-reset point therefore has roughly \(\pm 10\) mV of local
  trim margin before sign loss or overdrive.  This does not solve the
  array-level problem of discovering and distributing per-cell trims, but it
  rules out an immediately brittle single-point calibration.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-margin-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_refpert_ngspice.png}
  \caption{Ngspice reset-reference perturbation check for the calibrated
  shifted-gate reuse fixture.  The cell is the same low-common-mode
  \(V_{ZCM}=0.75\) V, \(96\,\mu\mathrm{m}\) forward-pair, \(10\) pF
  shifted-gate, \(+20/-20\) mV threshold-skew fixture as
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-trimmed-reuse-ngspice}.
  Instead of assuming perfect reset rails, the reference sources are driven by
  cycle-varying PWL commands and sampled after the physical reset switches.
  With common-mode reference commands of \(0.88,0.92,0.89\) V, the sampled
  shifted-gate common modes are \(0.909527,0.911545,0.890003\) V; finite
  reset settling/feedthrough is therefore visible, but the split differential
  remains \(-34.999,-34.998,-35.000\) mV and the stored activation only moves
  \(47.120,47.119,46.796\) mV.  Differential trim perturbations dominate the
  stored activation: requested \(-30,-35,-40\) mV trims sample as
  \(-33.690,-36.409,-39.999\) mV and store
  \(44.467,49.679,56.585\) mV, while requested \(-25,-35,-45\) mV trims
  sample as \(-32.382,-37.819,-44.999\) mV and store
  \(41.949,52.390,66.196\) mV.  The useful rule is that the reset reference
  network need not track ideal PWL commands exactly, but the sampled
  differential trim must stay inside the calibrated \(\pm 10\) mV window and
  reset must still clear \(z/h\) before each read.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-refpert-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_skew_trim_law_ngspice.png}
  \caption{Ngspice threshold-skew versus split-trim calibration-law check for
  the same shifted-gate reuse fixture.  The forward pair is swept through
  opposite threshold skews of \(10,15,20,25,\) and \(30\) mV by setting
  \(V_{TO,+}=0.55+\Delta\) V and \(V_{TO,-}=0.55-\Delta\) V, while the
  physical reset trim is scaled as
  \(-15,-25,-35,-45,\) and \(-55\) mV.  The reset switch network samples those
  split trims directly on the shifted-gate capacitors:
  \(-14.999,-24.999,-34.999,-44.998,\) and \(-54.998\) mV on the first reset,
  with common mode held at about \(0.899993\) V.  The calibrated repeated
  captures are essentially flat: mean stored activations are
  \(46.974,46.973,46.973,46.972,\) and \(46.972\) mV across the five skew
  magnitudes.  A deliberately under-trimmed \(30\) mV skew case using only the
  old \(-35\) mV trim stores \(8.467,8.448,\) and \(8.448\) mV, so the plot
  also shows the failure mode: the reset trim is not generic gain, it is a
  per-cell offset-cancellation code.  This does not provide the array-level
  calibration algorithm, but it demonstrates that the transistor-level reset
  primitive can express the needed trim law once the code is known.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-skew-trim-law-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_signed_skew_trim_law_ngspice.png}
  \caption{Ngspice signed threshold-skew trim-law check for the shifted-gate
  reuse fixture.  This family-completes
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-skew-trim-law-ngspice}
  by sweeping both forward-pair threshold-skew polarities.  The same physical
  split-reset network applies negative trims for the \(V_{TO,+}>V_{TO,-}\)
  polarity and positive trims for \(V_{TO,+}<V_{TO,-}\), with sampled shifted
  gate differentials matching the requested
  \(\pm 15,\pm 35,\) and \(\pm 55\) mV trims to the plotted precision and
  common mode remaining near \(0.90\) V.  The calibrated negative-trim branch
  stores about \(46.97\) mV for \(+10,+20,\) and \(+30\) mV skews; the
  calibrated positive-trim branch stores about \(66.20\) mV for
  \(-10,-20,\) and \(-30\) mV skews.  This is useful bidirectional signed
  correction, but not a perfectly symmetric gain law.  The deliberately
  under-trimmed severe cases show the opposite failure modes: \(+30\) mV skew
  with only \(-35\) mV trim stores \(8.45\) mV, while \(-30\) mV skew with
  only \(+35\) mV trim stores \(104.56\) mV.  The remaining physical-design
  problem is therefore residual polarity-dependent gain/calibration, not the
  existence of a signed hardware trim path.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-signed-skew-trim-law-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_polarity_calibration_ngspice.png}
  \caption{Ngspice polarity-offset calibration sweep for the severe
  \(30\) mV shifted-gate threshold-skew corners.  The previous signed-law plot
  showed that simply mirroring the trim sign leaves the \(V_{TO,+}<V_{TO,-}\)
  polarity around \(66.20\) mV while the \(V_{TO,+}>V_{TO,-}\) polarity lands
  around \(46.97\) mV.  Here the positive-skew branch is swept through
  \(-45,-55,\) and \(-65\) mV trims and stores mean activations
  \(27.720,46.972,\) and \(66.200\) mV.  The negative-skew branch is swept
  through \(+55,+65,\) and \(+75\) mV trims and stores
  \(66.207,46.978,\) and \(27.727\) mV.  The physical reset network samples
  all requested trims to the plotted precision and keeps the shifted-gate
  common mode near \(0.90\) V.  Therefore the opposite-polarity asymmetry is
  not a missing hardware path; it is a per-polarity trim-code offset.  In this
  fixture, \(+65\) mV positive trim aligns the \(-30\) mV skew branch with the
  \(-55\) mV negative-trim reference.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-polarity-calibration-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_positive_refpert_ngspice.png}
  \caption{Ngspice reset-reference perturbation check for the positive
  shifted-gate trim code from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-polarity-calibration-ngspice}.
  The fixture keeps the \(-30\) mV forward-pair threshold skew and uses the
  calibrated \(+65\) mV split reset.  Clean reset samples establish
  \(64.998,65.000,\) and \(65.000\) mV gate differentials and store
  \(46.999,46.967,\) and \(46.967\) mV.  Common-mode reset-reference commands
  of \(0.88,0.92,\) and \(0.89\) V settle imperfectly to
  \(0.909527,0.911544,\) and \(0.890003\) V, but the stored activation remains
  tight at \(47.128,47.130,\) and \(46.797\) mV.  Differential trim jitter is
  the sensitive rail: sampled \(62.381,67.820,\) and \(75.000\) mV trims store
  \(52.032,41.542,\) and \(27.716\) mV.  Thus the positive calibrated branch
  has the same practical rule as the earlier negative-trim branch: common-mode
  reset delivery has margin, but differential trim delivery must stay inside
  the calibrated window.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-positive-refpert-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_switch_corner_ngspice.png}
  \caption{Ngspice reset-switch threshold-corner check for the shifted-gate
  split-reset primitive.  The same physical MOS reset transmission gates now
  use explicit strong, nominal, weak, and skewed \(V_{TO}\) models while the
  forward-pair corners use the two calibrated severe trim cases from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-polarity-calibration-ngspice}:
  \(+30\) mV skew with \(-55\) mV trim and \(-30\) mV skew with \(+65\) mV
  trim.  Across reset-switch corners, the sampled trim error stays below
  \(0.004\) mV and shifted-gate common-mode error below \(0.009\) mV.  The
  calibrated stored activations remain essentially fixed:
  \(46.971\)--\(46.972\) mV for the negative-trim branch and
  \(46.978\) mV for the positive-trim branch, with sub-\(0.04\) mV cycle
  spread.  This means the current split-reset sizing is not visibly limited by
  reset-switch threshold corners in this Level-1 fixture; the sensitive rails
  remain forward-pair offset calibration and differential trim delivery, not
  ordinary reset TG \(V_{TO}\) variation.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-switch-corner-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_impedance_ngspice.png}
  \caption{Ngspice finite reset-reference source-impedance check for the same
  shifted-gate split-reset primitive.  This deck keeps the reset switches
  nominal but inserts series resistance between the ideal trim-reference
  source and each split reset rail.  The two severe calibrated branches are
  reused: \(+30\) mV forward-pair skew with \(-55\) mV trim and \(-30\) mV
  skew with \(+65\) mV trim.  Source impedances of \(0\) and \(1\,\mathrm{k}\Omega\)
  preserve the sampled trim to better than \(0.067\) mV and keep the stored
  activation at about \(47\) mV.  At \(10\,\mathrm{k}\Omega\), however, the
  first short pre-reset pulse is already too weak: the negative-trim branch
  samples only \(-31.782\) mV on the first reset and stores
  \(1.964,41.967,\) and \(44.688\) mV over the three cycles; the positive-trim
  branch samples \(37.563\) mV on the first reset and stores
  \(97.472,48.589,\) and \(45.160\) mV.  At \(100\,\mathrm{k}\Omega\) and
  above, the latch is effectively under-trimmed and returns to the known
  failure directions.  Thus the reset-switch devices themselves have corner
  margin, but the trim-reference driver must be a low-impedance local rail or
  the reset pulse must be lengthened before the split-reset calibration can be
  trusted in an array.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-impedance-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_width_ngspice.png}
  \caption{Ngspice first-reset pulse-width recovery sweep for the finite
  reset-reference source-impedance failure in
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-impedance-ngspice}.
  The same two severe calibrated branches are reused, but now the first
  pre-read reset active time is swept from \(120\) to \(400\) ns while the
  trim-reference source impedance is held at either \(10\,\mathrm{k}\Omega\)
  or \(100\,\mathrm{k}\Omega\).  At \(10\,\mathrm{k}\Omega\), the sampled trim
  error improves monotonically from \(23.218/27.437\) mV to
  \(3.688/4.359\) mV for the \(-55/+65\) mV trim branches, crossing the
  \(6\) mV trim-error gate at the longest pulse.  At
  \(100\,\mathrm{k}\Omega\), the same \(400\) ns reset remains badly
  under-driven: final trim errors are still \(41.818/49.421\) mV.  The
  first-cycle stored activation shows the remaining schedule limit.  The
  negative-trim \(10\,\mathrm{k}\Omega\) branch improves from \(1.964\) to
  \(24.868\) mV but does not reach the calibrated window, while the
  positive-trim branch falls from \(97.472\) to \(40.464\) mV.  Thus longer
  reset pulses can recover moderate trim delivery, but the first-cycle
  read/store schedule and high-impedance trim rails still need local
  buffering or additional timing margin before the split-reset calibration
  is array-ready.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-width-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_decap_ngspice.png}
  \caption{Ngspice passive local decoupling sweep for the same finite
  reset-reference source-impedance failure.  The source impedance is held at
  \(100\,\mathrm{k}\Omega\), the first reset active time is kept at \(120\)
  ns, and each split trim-reference rail gets a local capacitor from \(0\) to
  \(250\) pF.  These are passive capacitors with initial conditions matching
  the trim-reference rails; no behavioral buffer or runtime capacitor forcing
  is inserted.  The trim-error curves fall monotonically from
  \(50.389/59.550\) mV at \(0\) pF to \(3.097/3.660\) mV at \(250\) pF for the
  \(-55/+65\) mV trim branches, crossing the \(6\) mV gate.  The first-cycle
  stored activations recover from \(-47.035/135.835\) mV to
  \(40.942/53.933\) mV, which lands inside the calibrated activation window.
  Thus the high-source-impedance reset-reference failure is not an argument
  for an ideal trim driver; a local passive charge reservoir can supply the
  reset-edge gate charge, with the obvious area and refresh/settle-time cost.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-decap-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_recharge_ngspice.png}
  \caption{Ngspice repeated-reset recharge check for the passive local
  trim-reference reservoir in
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-decap-ngspice}.
  The source impedance remains \(100\,\mathrm{k}\Omega\), but the normal reuse
  schedule is restored so the shifted gate is reset, read, sampled, reset, and
  read again on the same physical nodes.  With \(250\) pF on each split
  trim-reference rail, the worst sampled trim error over all reset cycles is
  \(3.097/3.660\) mV for the \(-55/+65\) mV branches.  The three stored
  activations are \(40.942/41.221/41.551\) mV and
  \(53.933/53.555/53.165\) mV, respectively, so the passive reservoir stays in
  the calibrated activation band rather than helping only the first cycle.
  The practical rule is that the trim-reference reservoir must be local, but
  the \(100\,\mathrm{k}\Omega\) source can recharge it between the
  microsecond-spaced resets used here.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-recharge-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_startup_ngspice.png}
  \caption{Ngspice cold-start check for the same passive local
  trim-reference reservoir.  The fixture keeps the \(100\,\mathrm{k}\Omega\)
  source, \(250\) pF local reservoirs, \(5\) pF shifted-gate loads, and real
  complementary MOS reset switches, but varies the reservoir's initial charge
  before the first reset.  A cold reservoir badly under-delivers the split
  trim: the \(-55/+65\) mV branches sample only \(-5.379/+6.357\) mV of
  shifted-gate differential with common mode near \(0.106\) V.  After three
  local \(RC\) time constants they sample \(-51.500/+60.864\) mV with
  \(0.860\) V common mode, leaving \(3.500/4.136\) mV trim error; initialized
  reservoirs land at \(-53.927/+63.732\) mV with about \(1.1/1.3\) mV error.
  The design rule is therefore stricter than ``add a local decoupling cap'':
  the reservoir also needs startup precharge time or a low-impedance
  initialization path before the first calibrated reset.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-startup-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_pair_96u_shifted_gate_reset_ref_precharge_ngspice.png}
  \caption{Ngspice MOS startup-precharge recovery check for the cold passive
  local trim-reference reservoir from
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-startup-ngspice}.
  The reservoir capacitors still start at \(0\) V and the normal
  \(100\,\mathrm{k}\Omega\) trim sources remain connected, but a temporary
  \(300/900\,\mu\mathrm{m}\) NMOS/PMOS transmission-gate path precharges the
  local split rails before the ordinary MOS reset samples them into the
  \(5\) pF shifted-gate capacitors.  With no startup pulse the \(-55/+65\) mV
  trim branches reproduce the cold failure, leaving \(49.618/58.640\) mV
  trim error and \(0.106\) V common mode.  A \(5\) ns pulse is still marginal
  at \(8.161/9.652\) mV error.  A \(10\) ns pulse crosses the \(6\) mV
  trim-error gate with \(2.774/3.309\) mV error at \(0.869\) V common mode,
  and a \(20\) ns pulse reaches the initialized-reservoir regime with
  \(1.147/1.356\) mV error at \(0.896\) V common mode.  The circuit-level rule
  is therefore explicit: the passive reservoir fix is valid if startup
  includes a short low-impedance MOS precharge phase before the first
  calibrated reset.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-pair-96u-shifted-gate-reset-ref-precharge-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_update_forward_guard_forward_tail_bias_ngspice.png}
  \caption{Ngspice isolated forward-tail bias sweep for the same guarded MOS
  activation store.  The failed shared-bias experiment changed the read path
  too, so this test keeps the original global \(V_\mathrm{bias}=0.95\) V for
  the writer/read machinery and gives only the crossed forward pair tail NMOS
  its own bias source.  The read preactivation remains fixed at
  \(52.066\) mV for every case.  For isolated forward-tail biases of
  \(0.70,0.80,0.90,0.95,1.05,1.15,\) and \(1.25\) V, the immediate
  forward-load differentials are
  \(49.248,50.524,50.994,51.130,51.309,51.422,\) and \(51.544\) mV, and the
  guarded stored activations are
  \(49.657,50.752,51.221,51.355,51.653,51.756,\) and \(51.813\) mV.  The
  monotonic direction is correct, but the full sweep changes the stored value
  by only about \(2.16\) mV.  In this sizing, the useful activation-gain knob
  is therefore forward-pair/tail width, not tail-bias overdrive; a separate
  forward-tail bias rail is still useful for fine trim and bias isolation, but
  it should not be counted on for the large swing increase.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-update-forward-guard-forward-tail-bias-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_repeated_ngspice.png}
  \caption{Ngspice repeated-pulse accumulation characterization of the hybrid
  restored writer.  One stored \(r^+\) hidden-error rail drives one persistent
  weight-capacitor pair through seven active-low \(pacc\) pulses.  The
  restored gates remain near \(0.196\) V selected and \(1.731\) V
  complementary while the selected \(W^+\) capacitor accumulates nearly equal
  increments of about \(12.27\) mV per \(160\) ns pulse.  The post-pulse
  differential samples are \(12.3\), \(24.6\), \(36.8\), \(49.1\), \(61.4\),
  \(73.6\), and \(85.9\) mV; the complement \(W^-\) rail moves only about
  \(8\) nV.  This plot checks the online-training use case that one-shot
  writer plots do not cover: repeated local writes accumulate on the same
  capacitor pair without re-enabling the complement branch.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-repeated-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_alternating_ngspice.png}
  \caption{Ngspice alternating-sign cancellation characterization of the hybrid
  restored writer on one physical weight-capacitor pair.  The deck stores
  matched \(r^+\) and \(r^-\) hidden-error differentials of \(\pm98.5\) mV,
  restores the selected gates to about \(0.196\) V and the complement gates to
  about \(1.731\) V, then applies three \(W^+\) write pulses followed by four
  \(W^-\) write pulses.  The signed weight first accumulates
  \(12.3\), \(24.6\), and \(36.8\) mV, then the opposite-sign pulses cancel it
  back through \(24.6\), \(12.3\), approximately \(0\), and finally
  \(-12.3\) mV.  During the positive phase the \(W^-\) rail moves only at the
  nV scale; during the negative phase the \(W^+\) rail holds while \(W^-\)
  accumulates.  This is a stricter online-training check than
  Fig.~\ref{fig:mos-hidden-writer-restored-gate-hybrid-repeated-ngspice}
  because it proves the same capacitor pair can reverse update direction
  instead of only accumulating one sign.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-alternating-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_readback_ngspice.png}
  \caption{Ngspice readback and retention characterization for the hybrid
  restored writer.  The same matched \(r^+\)/\(r^-\) hidden-error stores and
  alternating restored writer stacks drive one \(W^+/W^-\) capacitor pair, but
  the capacitors are also continuously connected to a signed MOS synapse read
  slice.  Initially the positive and gate-swapped negative read components are
  balanced at about \(\pm120.2\,\mu\mathrm{A}\).  After the three \(W^+\)
  pulses, the stored differential is \(36.2\) mV and the net read current is
  \(+30.7\,\mu\mathrm{A}\).  After cancellation, the weight differential is
  \(-0.0006\) mV and the readback is \(-0.0005\,\mu\mathrm{A}\).  The extra
  \(W^-\) pulse leaves \(-12.1\) mV stored differential and
  \(-11.0\,\mu\mathrm{A}\) net read current.  During the \(4.55\)--\(9.5\,\mu s\)
  hold/read window the weight-cap drift is only about \(0.037\,\mu\mathrm{V}\)
  per rail and the net read current is unchanged at the plotted precision.
  This closes the loop from hybrid local write to actual signed synapse
  contribution and read-disturb hold, rather than only plotting the storage
  capacitors.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-readback-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_hidden_writer_restored_gate_hybrid_reuse_ngspice.png}
  \caption{Ngspice hidden-error reset/reuse characterization for the hybrid
  restored writer.  Unlike the previous matched-copy plots, this deck uses one
  physical \(C_{\delta^+}/C_{\delta^-}\) pair.  The first phase stores
  \(r^+\), writes \(W^+\), then a complementary transmission-gate reset clears
  only the hidden-error capacitors back to their neutral common mode.  The
  same pair is then rewritten as \(r^-\) and drives the opposite writer branch
  on the same persistent weight pair.  The stored hidden-error differential
  moves \(+98.5\) mV \(\to 0\) \(\to -98.5\) mV, with reset common mode
  returning to \(1.0399997\) V.  The restored selected gate swaps from the
  \(c_{\delta^+}\) side to the \(c_{\delta^-}\) side, both near \(0.196\) V,
  while the inactive restored gate stays near \(1.731\) V.  The reset phase
  does not disturb the first \(25.7\) mV \(W^+ - W^-\) step, and the second
  opposite-sign write cancels the persistent weight pair back to approximately
  zero.  This closes the hidden-error lifecycle gap for the current hybrid
  writer: sample-to-sample reuse is done by MOS reset and storage phases, not
  by Python reinitializing the error capacitors.}
  \label{fig:mos-hidden-writer-restored-gate-hybrid-reuse-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_reset_precharge_ngspice.png}
  \caption{Ngspice transient characterization of MOS reset/precharge for
  reusable state capacitors.  A complementary MOS transmission-gate reset first
  clears a deliberately mismatched \(z^+/z^-\) capacitor pair to common mode
  and also clears representative activation and hidden-error capacitor pairs.
  The synapse slice then writes a new signed preactivation, and a second reset
  clears that written state without Python forcing any of the capacitor nodes.}
  \label{fig:mos-reset-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_reset_width_ngspice.png}
  \caption{Ngspice reset pulse-width margin characterization.  Identical
  mismatched capacitor pairs are reset either by NMOS-only pass devices or by
  the complementary transmission-gate reset used in the cell target.  The
  transmission gate clears the differential residue much faster and keeps the
  post-reset common mode pinned near \(V_{CM}\), while the NMOS-only copy leaves
  larger residue and shifts common mode.  This validates the reset topology
  used for reusable preactivation, activation, and hidden-error capacitors.}
  \label{fig:mos-reset-width-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_reset_mismatch_ngspice.png}
  \caption{Ngspice threshold-corner characterization of the complementary MOS
  reset transmission gate.  Five reset copies clear the same deliberately
  mismatched capacitor pair under strong, nominal, weak, and skewed
  NMOS/PMOS-threshold corners.  With a \(400\) ns reset pulse, the residual
  \(|z^- - z^+|\) ranges from about \(29\) mV in the strong corner to
  \(53\) mV in the weak corner, while common-mode error stays below the mV
  scale.  This completes the reset robustness picture: pulse width and
  non-overlap still matter, but the complementary reset does not rely on one
  exact threshold corner to clear reusable state.}
  \label{fig:mos-reset-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_reset_write_timing_ngspice.png}
  \caption{Ngspice reset-release to synapse-write timing characterization.
  Each copy starts at the reset common mode, uses the same complementary reset,
  and receives a positive synapse write pulse at a different offset relative to
  reset release.  A write fully inside reset is strongly suppressed but not
  mathematically eliminated, a reset-edge write leaves a partial preactivation,
  and clean post-reset writes agree.  The plot therefore makes the reset/write
  non-overlap requirement explicit: reset and write devices can contend, so the
  training phase schedule should provide a real release-to-write margin.}
  \label{fig:mos-reset-write-timing-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_ngspice.png}
  \caption{Ngspice transient characterization of the four-quadrant MOS writer.
  The weight capacitors are biased in the same above-threshold voltage range
  used by the synapse tail-gate read path.  The selectivity panels exercise all
  four product branches: \(x^+\delta^+\) and \(x^-\delta^-\) step only
  \(W^+\), while \(x^+\delta^-\) and \(x^-\delta^+\) step only \(W^-\).  The
  pulse-width sweep verifies near-linear selected write magnitude for both
  \(W^+\) and \(W^-\) while the opposite inactive rail stays quiet.  The
  readback panel then uses the written \(W^+\) and \(W^-\) voltages as positive-
  and negative-weight synapse tail gates, verifying opposite signed contribution
  changes from the stored capacitor rails.}
  \label{fig:mos-writer-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_analog_gate_ngspice.png}
  \caption{Ngspice analog gate-strength characterization of the MOS writer.
  The selected same-sign branch uses active-low PMOS gates, so lowering either
  the activation gate or the hidden-error gate increases the selected
  \(W^+\) write current.  The sweep shows a monotone, compressed update curve
  over the tested gate range while the inactive \(W^-\) branch remains quiet.
  This is not an ideal four-quadrant multiplier; it is the usable analog
  operating window for a local coincidence writer whose magnitude is still
  controlled by the stored rails and phase pulse.}
  \label{fig:mos-writer-analog-gate-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_mismatch_ngspice.png}
  \caption{Ngspice PMOS-threshold mismatch characterization of the MOS writer.
  Three matched writer copies use \(V_{TO}=-0.50\), \(-0.55\), and
  \(-0.60\) V in the selected PMOS stacks while the same analog activation and
  hidden-error gates drive both signs.  The selected \(W^+\) and \(W^-\) steps
  remain symmetric and positive, decreasing from about \(10.4\) mV to
  \(6.0\) mV as threshold magnitude increases.  The inactive opposite rails
  stay at numerical noise.  This is the writer-side counterpart to the forward
  and synapse mismatch plots: threshold offsets change update gain, but do not
  by themselves flip the local update sign in this operating window.}
  \label{fig:mos-writer-mismatch-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_repeated_ngspice.png}
  \caption{Ngspice repeated-pulse characterization of the same MOS writer.  A
  train of ten active-low \(pacc\) coincidences emulates ten online
  \(\code{batch\_size=1}\) sample writes without resetting the weight
  capacitors.  The selected \(W^+\) and \(W^-\) rails accumulate monotonically
  and remain near-linear with pulse count over this operating window, while
  the inactive opposite rails stay pinned.  This is still a single-cell writer
  check, but it is closer to the update pattern required by continuous
  on-device training than a one-pulse selectivity test.}
  \label{fig:mos-writer-repeated-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_alternating_ngspice.png}
  \caption{Ngspice alternating-update characterization of the same MOS writer
  and synapse read path.  Starting from equal \(W^+\) and \(W^-\) rails, the
  net read contribution is near zero.  A train of same-sign writer pulses
  charges \(W^+\) and moves the same stored weight pair positive; a later train
  of opposite-sign pulses charges \(W^-\) and pulls the net read contribution
  back through cancellation to a negative value.  The stored rails are not
  erased between phases; the signed effect comes from differential
  \(W^+ - W^-\) readout.}
  \label{fig:mos-writer-alternating-ngspice}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\linewidth]{figures/mos_writer_retention_ngspice.png}
  \caption{Ngspice writer retention and read-disturb characterization.  A
  same-sign writer pulse charges \(W^+\), an opposite-sign writer pulse charges
  \(W^-\), and both written capacitor rails are then continuously used as
  synapse tail gates during a \(2.0\)--\(9.5~\mu\)s hold window.  In this
  Level-1 MOS testbench the written rails drift by only tens of nV and the
  signed read currents remain stable to the sub-nA level, so the plotted
  writer/read path is not disturbed by the gate-only synapse read.  This is a
  topology sanity check; real floating-gate, memristor, or leakage-dominated
  storage devices still need their own retention/read-disturb characterization.}
  \label{fig:mos-writer-retention-ngspice}
\end{figure}

\section{Trainable Cell Catalog}


The repository now treats a hidden block/channel as a
\(\code{TrainableDynamicalCell}\): a circuit-emittable learning object with
public input/output/learning ports, declared internal state, a learning
protocol, and characterization expectations.  The first ten concrete cells are
algorithmic reference candidates, not circuit diagrams.  They all share the
same state and phase contract shown in Fig.~\ref{fig:cell-template}; they differ
in the forward nonlinearity \(\phi\), the hidden synapse transfer \(S_W\), the
derivative gate, and the hidden-error feedback path.  These diagrams are kept to
define equations and ports only; transistor-level promotion starts from
Figs.~\ref{fig:mos-synapse-slice}--\ref{fig:mos-writer-cell} and from single-cell SPICE
characterization.

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      box/.style={draw, rounded corners=2pt, align=center, fill=lightgray,
        minimum width=2.7cm, minimum height=0.85cm, inner sep=4pt},
      statecap/.style={draw, cylinder, shape border rotate=90, aspect=0.28,
        minimum width=0.7cm, minimum height=0.85cm, fill=green!10,
        align=center},
      phase/.style={draw, rounded corners=2pt, align=center, fill=yellow!12,
        minimum width=1.65cm, minimum height=0.55cm, inner sep=2pt},
      arr/.style={-{Latex[length=2mm]}, semithick},
      fwd/.style={arr, forwardblue},
      bwd/.style={arr, backred},
      wr/.style={arr, writeorange},
      lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt}
    ]
    \node[box] (x) at (0,0) {input rail\\\(x_{bp}\)};
    \node[statecap] (w) at (0,-1.7) {\(C_W\)};
    \node[statecap] (b) at (1.45,-1.7) {\(C_b\)};
    \node[box] (sum) at (3.0,0) {weighted sum\\\(z=b+\sum S_W(W)x\)};
    \node[box] (phi) at (6.2,0) {activation\\\(h^\star=\phi(z)\)};
    \node[statecap] (h) at (8.7,0) {\(C_h\)};
    \node[box] (score) at (11.2,0) {readout\\scores};

    \node[box] (err) at (11.2,-2.1) {output error\\\(d=t-\hat y\)};
    \node[box] (fb) at (8.2,-2.1) {feedback\\\(r=\sum dF\)};
    \node[box] (gate) at (5.45,-2.1) {derivative gate\\\(\delta h^\star=r\phi'(z)\)};
    \node[statecap] (dh) at (3.0,-2.1) {\(C_{\delta h}\)};
    \node[statecap] (v) at (11.2,-4.0) {\(C_V\)};

    \node[phase] (pact) at (5.7,1.15) {\(pact\)};
    \node[phase] (pbwd) at (5.7,-3.1) {\(pbwd\)};
    \node[phase] (pacc) at (5.7,-4.55) {\(pacc\)};

    \draw[fwd] (x) -- (sum);
    \draw[fwd] (w) |- (sum);
    \draw[fwd] (b) |- (sum);
    \draw[fwd] (sum) -- (phi);
    \draw[fwd] (phi) -- node[lbl, above] {store} (h);
    \draw[fwd] (h) -- (score);
    \draw[fwd] (score) -- (err);

    \draw[bwd] (err) -- (fb);
    \draw[bwd] (fb) -- (gate);
    \draw[bwd] (sum) |- (gate);
    \draw[bwd] (gate) -- (dh);

    \draw[wr] (dh) |- (w);
    \draw[wr] (dh) |- (b);
    \draw[wr] (err) -- (v);
    \draw[wr] (h) |- (v);

    \draw[fwd] (pact) -- (phi);
    \draw[bwd] (pbwd) -- (gate);
    \draw[wr] (pacc) -- (6.9,-4.55) |- (v);
    \draw[wr] (pacc) -- (3.6,-4.55) |- (dh);
  \end{tikzpicture}}
  \caption{Algorithmic contract template used by the ten reference candidates;
  this is not a circuit schematic.  Forward state \(h\), backward state
  \(\delta h\), local weights and biases, and readout weights correspond to
  capacitor state in the MOS target.  Candidate equations replace the
  activation, synapse transfer, derivative gate, and feedback rule while
  preserving the same public-port and phase contract.}
  \label{fig:cell-template}
\end{figure}

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      box/.style={draw, rounded corners=2pt, align=center, fill=lightgray,
        minimum width=2.9cm, minimum height=0.82cm, inner sep=4pt},
      statecap/.style={draw, cylinder, shape border rotate=90, aspect=0.28,
        minimum width=0.68cm, minimum height=0.8cm, fill=green!10,
        align=center},
      arr/.style={-{Latex[length=2mm]}, semithick},
      fwd/.style={arr, forwardblue},
      bwd/.style={arr, backred},
      lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt}
    ]
    \node[box] (err1) at (0,0) {class error\\\(d_k\)};
    \node[statecap] (v1) at (2.5,0.9) {\(C_V\)};
    \node[box] (fb1) at (5.0,0) {backprop feedback\\\(r_{bc}=\sum_k d_kV_{kbc}\)};
    \node[box] (dh1) at (9.0,0) {hidden delta\\\(\delta h=r\phi'(z)\)};
    \draw[bwd] (err1) -- (fb1);
    \draw[bwd] (v1) -- node[lbl, above] {trainable} (fb1);
    \draw[bwd] (fb1) -- (dh1);
    \node[font=\bfseries\small] at (4.7,1.65) {BACKPROP\_LOCAL family};

    \node[box] (err2) at (0,-2.6) {class error\\\(d_k\)};
    \node[statecap] (b2) at (2.5,-1.7) {\(B\)};
    \node[box] (fb2) at (5.0,-2.6) {DFA feedback\\\(r_{bc}=\sum_k d_kB_{kbc}\)};
    \node[box] (dh2) at (9.0,-2.6) {hidden delta\\\(\delta h=r\phi'(z)\)};
    \draw[bwd] (err2) -- (fb2);
    \draw[bwd] (b2) -- node[lbl, above] {fixed random} (fb2);
    \draw[bwd] (fb2) -- (dh2);
    \node[font=\bfseries\small] at (4.7,-0.95) {DFA family};
  \end{tikzpicture}}
  \caption{Backward-path variants.  Most catalog cells use the trainable
  readout weights as the feedback path, possibly clipped or sign-compressed.
  The DFA cell keeps the same forward and write machinery but replaces
  transposed readout feedback with a fixed random projection.}
  \label{fig:cell-feedback}
\end{figure}

Figure~\ref{fig:cell-transfer-families} groups the main reference-equation
families.  These are not hardware blocks.  A family becomes a circuit candidate
only after a MOS replacement passes update-alignment and disturb tests.

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      fam/.style={draw, rounded corners=2pt, align=left, fill=lightgray,
        text width=4.4cm, minimum height=2.0cm, inner sep=6pt}
    ]
    \node[fam] (tanh) at (0,0) {
      \textbf{Stored tanh}\\
      \(\phi(z)=\tanh z\)\\
      \(\phi'(z)=1-h^2\)\\
      \(S_W(w)=w\) or \(s\tanh(w/s)\)
    };
    \node[fam] (diff) at (5.25,0) {
      \textbf{Diff-pair tanh proxy}\\
      same \(\tanh\) forward branch\\
      soft-clipped local weights\\
      clipped readout feedback
    };
    \node[fam] (hard) at (10.5,0) {
      \textbf{Hard-tanh branches}\\
      signed clip or leaky saturation\\
      hard-clipped hidden synapses\\
      floor derivative option
    };
    \node[fam] (relu) at (2.6,-2.9) {
      \textbf{ReLU-like branches}\\
      leaky ReLU, clipped ReLU\\
      nonnegative output when clipped\\
      signed errors and writes remain
    };
    \node[fam] (soft) at (7.9,-2.9) {
      \textbf{Softplus branches}\\
      \(\phi(z)=\beta^{-1}\log(1+e^{\beta z})\)\\
      or centered softplus\\
      smooth sigmoid derivative
    };
  \end{tikzpicture}}
  \caption{Transfer-function families represented in the ten-cell catalog.
  The strongest first smoke was the differential-pair-inspired tanh/soft-clip
  branch, which is attractive because a MOS differential pair naturally gives a
  tanh-like transfer and soft saturation.}
  \label{fig:cell-transfer-families}
\end{figure}

All ten catalog cells use the same online local-learning skeleton:
\[
  z_{bc}=b_{bc}+\sum_p S_W(W_{bcp})\,x_{bp},
  \qquad
  h_{bc}=\phi(z_{bc}),
\]
\[
  s_k=\sum_{b,c}S_V(V_{kbc})\,h_{bc}+o_k,
  \qquad
  d_k=t_k-\operatorname{softmax}(s/T)_k,
\]
\[
  r_{bc}=\sum_k d_k F_{kbc},
  \qquad
  \delta h_{bc}=r_{bc}\,g(z_{bc},h_{bc}),
\]
\[
  \Delta W_{bcp}=\eta\,\delta h_{bc}x_{bp},
  \qquad
  \Delta b_{bc}=\eta\,\delta h_{bc},
  \qquad
  \Delta V_{kbc}=\eta\,\alpha_V d_k h_{bc}.
\]
The cell-specific choices are the activation \(\phi\), derivative gate \(g\),
hidden synapse transfer \(S_W\), readout transfer \(S_V\), and feedback matrix
\(F\).  The common transfer primitives are:
\[
  S_{\mathrm{linear}}(w)=w,\qquad
  S_{\mathrm{tanhclip}}(w;s)=s\tanh(w/s),\qquad
  S_{\mathrm{hardclip}}(w;s)=\operatorname{clip}(w,-s,s),
\]
\[
  F_{\mathrm{readout}}=S_V(V),\qquad
  F_{\mathrm{clip}}=c\tanh(S_V(V)/c),\qquad
  F_{\mathrm{DFA}}=B_{\mathrm{fixed}}.
\]

\begin{table}[H]
  \centering
  \caption{Governing equations for the ten catalog cells.  These are the
  equations exercised by the unit characterization and by the NumPy MNIST
  benchmark.}
  \label{tab:cell-equations}
  \scriptsize
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.23\linewidth}
                  >{\raggedright\arraybackslash}p{0.27\linewidth}
                  >{\raggedright\arraybackslash}p{0.27\linewidth}
                  >{\raggedright\arraybackslash}p{0.14\linewidth}}
    \toprule
    Cell & Forward activation \(\phi(z)\) and gate \(g\) & Synapse/feedback equations & Unit validation \\
    \midrule
    \code{stored\_tanh\_tanhclip} &
    \(\phi=\tanh z,\; g=1-h^2\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=V,\; \alpha_V=0.5\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{stored\_tanh\_linear} &
    \(\phi=\tanh z,\; g=1-h^2\) &
    \(S_W(w)=w,\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{diffpair\_tanh\_softclip} &
    \(\phi=\tanh z,\; g=1-h^2\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;0.75),\; S_V(w)=w,\; F=0.25\tanh(V/0.25)\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{signed\_hardtanh} &
    \(\phi=\operatorname{clip}(z,-1,1),\;
      g=\mathbf{1}_{|z|\le1}\) &
    \(S_W=S_{\mathrm{hardclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{leaky\_hardtanh} &
    \(\phi=z\) for \(|z|\le1\), else
    \(\operatorname{sign}(z)(1+0.05(|z|-1))\);
    \(g=0.02+0.98\,[\mathbf{1}_{|z|\le1}\vee0.05]\) &
    \(S_W=S_{\mathrm{hardclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{leaky\_relu} &
    \(\phi=\max(z,0)+0.05\min(z,0),\;
      g=1\) for \(z\ge0\), \(0.05\) otherwise &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{clipped\_relu} &
    \(\phi=\operatorname{clip}(z,0,1),\;
      g=0.02+0.98\mathbf{1}_{0\le z\le1}\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{softplus} &
    \(\phi=\beta^{-1}\log(1+e^{\beta z}),\;
      g=(1+e^{-\beta z})^{-1},\; \beta=4\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{centered\_softplus} &
    \(\phi=\beta^{-1}[\log(1+e^{\beta z})-\log2],\;
      g=(1+e^{-\beta z})^{-1},\; \beta=4\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=V\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \addlinespace
    \code{dfa\_tanh\_fixed\_feedback} &
    \(\phi=\tanh z,\; g=1-h^2\) &
    \(S_W=S_{\mathrm{tanhclip}}(w;1.0),\; S_V(w)=w,\; F=B_{\mathrm{fixed}}\) &
    monotone; \(g\ge0\); update sign/cosine aligned \\
    \bottomrule
  \end{tabular}
\end{table}

The automated tests validate the equations at the contract and single-cell
levels.  The contract-validity test checks that all ten cells declare valid
ports, signedness, state roles, and protocol requirements.  The
learning-alignment test runs the characterization equations over a
deterministic operating grid:
\(\phi(z)\) must be monotone on \(z\in[-2.5,2.5]\), the derivative gate must be
nonnegative, the hidden synapse transfer must remain finite on
\([-4,4]\), and the synthetic local update must have positive cosine and
perfect sign alignment with the reference rule
\[
  \operatorname{sign}(\Delta W)=\operatorname{sign}(\delta h\,x).
\]
The MNIST benchmark then experimentally exercises the same equations in a
small online classifier.  It is a real-data algorithm smoke, not a SPICE or
transistor validation.

\begin{table}[H]
  \centering
  \caption{Ten reference local-feature equations and their real-MNIST smoke
  result.  The benchmark used \(10\times10\), block 4, stride 2, 2 channels,
  q16 inputs, 512 online updates, 1000 evaluation images, seeded random order,
  and no local-PCA.}
  \label{tab:cell-catalog}
  \scriptsize
  \begin{tabular}{llllr}
    \toprule
    Cell & Activation & Hidden synapse & Feedback path & Final acc. \\
    \midrule
    \code{diffpair\_tanh\_softclip} & tanh & tanh-clipped, \(s=0.75\) & clipped readout & \(56.6\%\) \\
    \code{dfa\_tanh\_fixed\_feedback} & tanh & tanh-clipped & fixed random \(B\) & \(54.2\%\) \\
    \code{stored\_tanh\_tanhclip} & tanh & tanh-clipped & readout & \(50.4\%\) \\
    \code{stored\_tanh\_linear} & tanh & linear & readout & \(46.1\%\) \\
    \code{leaky\_relu} & leaky ReLU & tanh-clipped & readout & \(42.7\%\) \\
    \code{softplus} & softplus & tanh-clipped & readout & \(41.8\%\) \\
    \code{centered\_softplus} & centered softplus & tanh-clipped & readout & \(38.3\%\) \\
    \code{clipped\_relu} & clipped ReLU & tanh-clipped & readout & \(37.8\%\) \\
    \code{leaky\_hardtanh} & leaky hard-tanh & hard-clipped & readout & \(35.8\%\) \\
    \code{signed\_hardtanh} & signed hard clip & hard-clipped & readout & \(24.6\%\) \\
    \bottomrule
  \end{tabular}
\end{table}

This catalog changes the promotion strategy.  Instead of replacing the working
stored tanh neuron with an arbitrary transistor ReLU, each candidate must keep
the same cell contract and prove four local properties before a full SPICE
MNIST run: monotone forward transfer, nonnegative derivative gate over the
declared operating range, signed update alignment, and acceptable state hold
and read disturb.  The current best promotion target is
\(\code{diffpair\_tanh\_softclip}\), because it both ranked first in the real
MNIST behavioral smoke and maps naturally onto a differential-pair
implementation.

\section{Phase Schedule}

The runner uses a repeated sample cycle with separate guide rails for forward
settling, activation storage, score storage, error/backward formation, and
direct accumulation/write.  In the high-accuracy behavioral run the update is
direct:
there are no gradient-accumulator capacitors and no apply-once batch phase.
This is why the run uses \(\mathrm{batch\_size}=1\) and 6000 distinct online
updates.

Pulse-gated clocks were used instead of listing every clock edge as long PWL
tables.  Non-constant learning rate was also moved into an analytic in-deck
control expression:
\[
  \eta(n): 0.3 \rightarrow 0.06
\]
by linear decay over the 6000 updates.  These are source-complexity reductions,
not training shortcuts: the deck still sees time-varying control rails, while
the neural state remains inside SPICE.

\section{Signed Quantities}

A ReLU activation can be single-ended and nonnegative after rectification, but
the working system still needs signed transport in several places.  Synapse
contributions, preactivations, errors, hidden deltas, and weight updates are
signed.  The clean physical representation is therefore differential or
equivalent positive/negative storage for those quantities:
\[
  u = u^+ - u^-,
  \qquad
  \Delta w = \Delta w^+ - \Delta w^-.
\]
Positive-weight branches contribute to the positive summing rail and
negative-weight branches contribute to the negative summing rail.  Backward
error rails and update rails follow the same rule.

The final high-accuracy local-feature run uses tanh hidden activations rather
than a literal ReLU.  The design lesson from the neuron/synapse screens is
still the same: rectification can simplify activation outputs, but it does not
remove signed transport from the synapse, score, error, hidden-delta, or write
paths.  In the q16 target-topology screens, tanh hidden activations with
tanh-clipped local synapses were more robust than leaky ReLU, softplus, and
differential clipped-ReLU alternatives at the longer online horizons.

\section{Input And Source-Cost Choices}

The current deck includes source-cost optimizations that preserve the execution
contract, but not all of them are acceptable for the final goal.

\paragraph{q16 input rails.}
The \(10\times10\) input pixels are quantized to 16 levels before waveform
generation.  Python is still only driving input rails; it is not computing
network state.  Quantization reduces the number of source transitions while
preserving enough accuracy headroom for the high-accuracy branch.

\paragraph{Rejected local-PCA sample order.}
The \(90.15\%\) run used a local-PCA ordering inside 512-sample windows to
reduce input PWL switching.  Although this does not read or write neural state,
it is still an unrealistic training assumption: the data stream is chosen using
a structure-aware preprocessing pass whose purpose is simulator convenience.
It should therefore not count toward the final goal.  Future headline runs
should use the stable dataset order, a seeded random order, or another
training-realistic order that is chosen independently of source-waveform
compression.

\section{Current Evidence}

No final-goal result is claimed yet.  Table~\ref{tab:headline} records the
strongest high-accuracy run because it is useful architectural evidence, but it
is disqualified from the final objective for two reasons: the neuron/write cells
are behavioral current equations, and the training order uses local-PCA source
compression.  The compact artifacts are:
\begin{center}
\small
\path{results/tables/spice_mnist_local_feature_phase_codex_q16_6k_localpca512_strict_u6000_eval10k_summary.json}
\end{center}
and its corresponding equivalence/metrics CSV.

\begin{table}[H]
  \centering
  \caption{High-accuracy behavioral/local-PCA run.  This is evidence, not a
  final-goal result.}
  \label{tab:headline}
  \begin{tabular}{ll}
    \toprule
    Field & Value \\
    \midrule
    Simulator & Xyce 7.10 \\
    Architecture & phase-resolved transient local-feature readout \\
    Topology & \(10\times10\), block 4, stride 2, 2 channels \\
    Classes & 10 \\
    Train samples / updates & 6000 / 6000 \\
    Batch size & 1 \\
    Eval samples & 10,000 MNIST test images \\
    Initial eval accuracy & \(10.89\%\) \\
    Final eval accuracy & \(\mathbf{90.15\%}\) \\
    Improvement & \(+79.26\) percentage points \\
    Reference mode & none \\
    Python weight updates between samples & false \\
    Python checkpointing between samples & false \\
    Random initialization & true \\
    Execution contract met & true \\
    Final-goal status & disqualified: behavioral writers and local-PCA order \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table}[H]
  \centering
  \caption{Key knobs and simulation cost for the disqualified high-accuracy
  run.}
  \label{tab:cost}
  \begin{tabular}{ll}
    \toprule
    Field & Value \\
    \midrule
    Input mode & PWL, 16 quantization levels \\
    Train order & local-PCA, window 512, not final-goal acceptable \\
    Hidden activation / local synapse & tanh / tanh-clipped \\
    Readout synapse & linear \\
    Learning rate & 0.3, linear decay to \(0.2\times\) \\
    LR control & analytic in-deck expression \\
    Readout update scale & 0.5 \\
    Output bias update scale & 0.0, frozen \\
    Softmax temperature & 2.0 \\
    Phase clock mode & pulse-gated \\
    Hidden activation/delta state & stored / stored \\
    Score/output delta state & stored / node \\
    Auxiliary algebraic source count & 0 \\
    Printed state vectors & 864 \\
    Estimated transient points & 16,803 \\
    Total source PWL points & 475,007 \\
    Pixel source PWL points & 465,518 \\
    Target source PWL points & 9,489 \\
    Phase wall time & 4,884.4 s \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Design Evidence}

The current architecture was selected by bounded screens and strict promotions,
but the useful conclusions are short:
\begin{enumerate}
  \item A tiny \(4\times4\), block-2, stride-2, 1-channel transient verified
    continuous-state correctness: a one-transient online SPICE run matched the
    trusted batch-op update direction without Python checkpointing.
  \item The target \(10\times10\), block-4, stride-2, 2-channel topology was
    the best balance of feature capacity, state count, and strict transient
    cost.
  \item Tanh hidden units with tanh-clipped local synapses were the most robust
    long-horizon branch among the tested neuron/synapse choices.  Differential
    clipped-ReLU was competitive early but trailed at longer horizons; softplus
    and leaky ReLU were weaker in this setup.
  \item q16 input quantization reduced waveform pressure while preserving
    learning.  Local-PCA ordering reduced waveform pressure further, but it is
    now treated as a disallowed simulator convenience rather than a valid
    training assumption.
  \item Pulse-gated clocks and analytic LR control removed avoidable PWL
    sources.  Pixel input transitions remain the dominant source-cost term.
  \item The 1024-update and 2048-update strict promotions showed meaningful
    full-test learning under the execution contract.  The 6000-update run
    showed that the architecture has enough learning headroom to reach
    \(90.15\%\), but it does not close the final objective because of the
    behavioral writer/neuron cells and local-PCA order.
\end{enumerate}

The most important enablers were not any single magic neuron.  They were the
combination of (1) enough local feature capacity in the \(10\times10\),
block-4, stride-2, 2-channel topology; (2) a stable signed update rule with
stored hidden activations and hidden deltas; (3) tanh/tanh-clipped nonlinearities
that avoided the dead/one-sided behavior seen in several ReLU-like screens;
(4) frozen output bias, readout scale \(0.5\), and softmax temperature \(2.0\),
which kept the output error and readout writes in a useful range; and (5) source
compression that made the long strict transient runnable.  The local-PCA part
of that compression is not acceptable for the final result.

\section{What The Result Does And Does Not Claim}

The current evidence establishes that a high-performing SPICE training path can
be moved from Python-orchestrated checkpoint/update chunks into a continuous,
stateful SPICE simulation for the local-feature architecture.  The
high-accuracy run starts from random weights, performs online updates inside
one transient, and crosses \(90\%\) on full MNIST test accuracy.

It does not establish the final goal.  First, the update cells in this deck are
behavioral current equations standing in for the transistor writer blocks that
earlier device-level experiments explored.  Second, the \(90.15\%\) run used
local-PCA sample ordering, which should be removed from the headline path
because it is not a realistic training assumption.  A future device-level
version should preserve the same execution contract while replacing the
behavioral kernels with characterized MOS differential synapses, error rails,
and signed write branches, and while training under a realistic sample order.

\section{Training Faster And Scaling Further}

The main open problem is still the full objective: device-level training,
realistic sample order, one uninterrupted transient, \(\mathrm{batch\_size}=1\),
and \(>90\%\) full-test MNIST accuracy.  Cost is the immediate practical
blocker.  The disqualified 6000-update run spent most of its source complexity
on pixel waveforms: 465,518 of 475,007 PWL points came from pixel sources.
That makes the first speed target input delivery, not the number of printed
state vectors.

\paragraph{Near-term simulation speedups.}
The most plausible speedups are:
\begin{enumerate}
  \item replace per-pixel PWL rails with a simulator-friendly encoded input
    source or sample-and-hold front end;
  \item replace the local-PCA source-compression trick with realistic input
    compression that is independent of the sample ordering;
  \item avoid printing or measuring any state not needed for final diagnostics;
  \item use strict small-horizon equivalence runs to validate knobs before
    spending multi-thousand-update Xyce runs;
  \item keep analytic controls and pulse-gated clocks, since explicit PWL
    control rails add cost without adding learning value.
\end{enumerate}

\paragraph{Architecture scale-up.}
The state count for the private local-feature family scales as
\[
  N_{\mathrm{state}}
  \approx
  B C(P+1) + KBC,
\]
where \(B\) is the number of block positions, \(C\) channels, \(P\) pixels per
block, and \(K\) classes.  Increasing image size, reducing stride, or adding
channels therefore grows both local feature state and readout state.  Scaling
should be done by a Pareto ladder: increase one axis at a time, require a cheap
reference endpoint above target, then promote only bounded strict SPICE runs.

\paragraph{Circuit scale-up.}
The behavioral neuron/write kernels should be replaced by transistor-level
blocks only after the replacement passes three local tests:
\begin{enumerate}
  \item forward transfer matches the stored tanh or chosen replacement over
    the operating voltage range;
  \item backward delta transport has positive alignment with the reference
    hidden delta;
  \item signed write currents move \(C_W\) and \(C_b\) in the same direction as
    \(\delta h\,x\) without excessive read disturb or rail pinning.
\end{enumerate}

Only after those local tests pass should the transistor cell replace the
behavioral current kernel in a full MNIST strict transient.  Otherwise the
system will be debugging device nonidealities and architecture scaling at the
same time.

\end{document}
